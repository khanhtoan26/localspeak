---
phase: 03-json-mode-ielts-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/gemini-feedback.ts
  - apps/api/src/config/env.ts
  - apps/api/.env.example
autonomous: true
requirements: [GEM-01, GEM-02]
must_haves:
  truths:
    - "GeminiFeedbackRequestSchema has typed fields for referenceText, pronunciationBand, fluencyBand, wpm, pauseRatio, weakWords, weakPhonemePatterns, notablePauses"
    - "GeminiFeedbackResponseSchema has strict typed fields for pronunciationBand, fluencyBand, topErrors (array of {word, phoneme, explanation}), drills, summary"
    - "ApiEnvSchema no longer requires SUPABASE_URL or SUPABASE_SECRET_KEY"
    - "ApiEnvSchema has optional GEMINI_MODEL with default gemini-2.0-flash"
  artifacts:
    - path: "packages/contracts/src/gemini-feedback.ts"
      provides: "Enriched request/response Zod schemas for Gemini feedback"
      contains: "GeminiFeedbackRequestSchema"
    - path: "apps/api/src/config/env.ts"
      provides: "Cleaned env schema with GEMINI_MODEL"
      contains: "GEMINI_MODEL"
  key_links:
    - from: "packages/contracts/src/gemini-feedback.ts"
      to: "packages/contracts/src/index.ts"
      via: "barrel re-export"
      pattern: "export.*gemini-feedback"
---

<objective>
Enrich the Gemini feedback contract schemas and clean up env configuration.

Purpose: Establish the typed contracts that Plan 02 (backend service) and Plan 03 (frontend) depend on, and remove dead Supabase env vars that block clean startup.
Output: Enriched `gemini-feedback.ts` with proper Zod schemas, cleaned `env.ts` without Supabase, updated `.env.example`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-json-mode-ielts-feedback/03-CONTEXT.md
@.planning/phases/03-json-mode-ielts-feedback/03-RESEARCH.md

<interfaces>
<!-- From packages/contracts/src/json-analysis.ts (types that feed request schema): -->
From JsonAnalysisResponseSchema fields:
- summary.pronunciationBand: number
- summary.fluencyBand: number
- summary.wpm: number (int, nonneg)
- summary.pauseRatio: number (nonneg)
- extracted.referenceText: string
- words[]: { word, score, band, ... }
- weakPhonemePatterns[]: { arpabet, ipaExamples, averageScore, weakOccurrenceCount, exampleWords }
- fluency.notablePauses[]: { duration, severity, beforeWord, afterWord, ... }

From packages/contracts/src/index.ts:
```typescript
export * from "./gemini-feedback";
```
(Already re-exported — no barrel change needed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich GeminiFeedbackRequestSchema and GeminiFeedbackResponseSchema</name>
  <files>packages/contracts/src/gemini-feedback.ts</files>
  <read_first>
    - packages/contracts/src/gemini-feedback.ts
    - packages/contracts/src/json-analysis.ts (lines 100-200 for field shapes)
    - .planning/phases/03-json-mode-ielts-feedback/03-RESEARCH.md (lines 349-397 for exact schema design)
  </read_first>
  <action>
Replace the entire content of `packages/contracts/src/gemini-feedback.ts` with properly typed schemas per D-03, D-06, D-19, D-20, D-21:

```typescript
import { z } from "zod";

// --- Request schema (what frontend sends to POST /api/gemini-feedback) ---

export const GeminiFeedbackRequestSchema = z.strictObject({
  referenceText: z.string().min(1),
  pronunciationBand: z.number(),
  fluencyBand: z.number(),
  wpm: z.number().int().nonnegative(),
  pauseRatio: z.number().nonnegative(),
  weakWords: z.array(
    z.strictObject({
      word: z.string().min(1),
      score: z.number().min(0).max(1),
    }),
  ),
  weakPhonemePatterns: z.array(
    z.strictObject({
      arpabet: z.string().min(1),
      ipaExamples: z.array(z.string()),
      averageScore: z.number().min(0).max(1),
      exampleWords: z.array(z.string()),
    }),
  ),
  notablePauses: z.array(
    z.strictObject({
      duration: z.number(),
      severity: z.enum(["natural", "noticeable", "critical"]),
      beforeWord: z.string(),
      afterWord: z.string(),
    }),
  ),
});

// --- Response schema (what Gemini returns, validated by Zod) ---

export const TopErrorSchema = z.strictObject({
  word: z.string().min(1),
  phoneme: z.string().min(1),
  explanation: z.string().min(1),
});

export const GeminiFeedbackResponseSchema = z.strictObject({
  pronunciationBand: z.number().min(0).max(9),
  fluencyBand: z.number().min(0).max(9),
  topErrors: z.array(TopErrorSchema).length(3),
  drills: z.array(z.string().min(1)).length(3),
  summary: z.string().min(1),
});

// --- Type exports ---

export type GeminiFeedbackRequest = z.infer<typeof GeminiFeedbackRequestSchema>;
export type GeminiFeedbackResponse = z.infer<typeof GeminiFeedbackResponseSchema>;
export type TopError = z.infer<typeof TopErrorSchema>;
```

Key design choices:
- `z.strictObject` (not `z.looseObject`) for strict field validation per D-07
- `topErrors` uses `.length(3)` to enforce exactly 3 per D-19
- `drills` uses `.length(3)` to enforce exactly 3 per D-20
- `weakWords` is an array of `{word, score}` pairs extracted from words with band="weak"
- `weakPhonemePatterns` matches existing `WeakPhonemePatternSchema` shape minus `weakOccurrenceCount` (not needed for prompt)
- `notablePauses` carries duration, severity, and context words for prompt construction
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && npx tsc --noEmit -p packages/contracts/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/gemini-feedback.ts` contains `z.strictObject({` (not `z.looseObject`)
    - File contains `referenceText: z.string().min(1)`
    - File contains `weakWords: z.array(`
    - File contains `weakPhonemePatterns: z.array(`
    - File contains `notablePauses: z.array(`
    - File contains `topErrors: z.array(TopErrorSchema).length(3)`
    - File contains `drills: z.array(z.string().min(1)).length(3)`
    - File contains `export type GeminiFeedbackRequest`
    - File contains `export type GeminiFeedbackResponse`
    - File contains `export type TopError`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>GeminiFeedbackRequestSchema has all typed fields from JsonAnalysisResponse. GeminiFeedbackResponseSchema enforces exact 3 topErrors and 3 drills with proper nested types. All types exported.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Supabase env vars and add GEMINI_MODEL to ApiEnvSchema</name>
  <files>apps/api/src/config/env.ts, apps/api/.env.example</files>
  <read_first>
    - apps/api/src/config/env.ts
    - apps/api/.env.example
    - .planning/phases/03-json-mode-ielts-feedback/03-RESEARCH.md (lines 457-471 for target env.ts)
  </read_first>
  <action>
Replace `apps/api/src/config/env.ts` with this cleaned version (per D-16, D-17, and the "Drop Supabase" architectural decision):

```typescript
import { z } from "zod";

export const ApiEnvSchema = z.object({
  NODE_ENV: z.enum(["development", "test", "production"]).default("development"),
  PORT: z.coerce.number().int().positive().default(3001),
  GEMINI_API_KEY: z.preprocess(
    (value) => (typeof value === "string" ? value.trim() : ""),
    z.string().min(1, "GEMINI_API_KEY is required"),
  ),
  GEMINI_MODEL: z.string().default("gemini-2.0-flash"),
});

export type ApiEnv = z.infer<typeof ApiEnvSchema>;

export function validateApiEnv(config: Record<string, unknown>): ApiEnv {
  return ApiEnvSchema.parse(config);
}
```

Update `apps/api/.env.example` to:
```
PORT=3001
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
```

Changes:
- REMOVED: `SUPABASE_URL`, `SUPABASE_SECRET_KEY` (dead code from dropped Supabase decision)
- REMOVED: `SUPABASE_SECRET_KEY_REQUIRED` constant
- ADDED: `GEMINI_MODEL` with default `"gemini-2.0-flash"` (optional, so existing .env files still work)
- KEPT: `validateApiEnv` function unchanged in signature
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30 && grep -c "SUPABASE" apps/api/src/config/env.ts && grep "GEMINI_MODEL" apps/api/src/config/env.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/config/env.ts` does NOT contain the string `SUPABASE`
    - `apps/api/src/config/env.ts` contains `GEMINI_MODEL: z.string().default("gemini-2.0-flash")`
    - `apps/api/src/config/env.ts` contains `GEMINI_API_KEY`
    - `apps/api/src/config/env.ts` contains `export function validateApiEnv`
    - `apps/api/.env.example` contains `GEMINI_MODEL=gemini-2.0-flash`
    - `apps/api/.env.example` does NOT contain `SUPABASE`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>ApiEnvSchema cleaned of Supabase references. GEMINI_MODEL added as optional with default. .env.example updated. App can start without Supabase env vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → runtime | Environment variables loaded at startup; malformed values rejected by Zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | env.ts | mitigate | GEMINI_API_KEY validated as non-empty string; never logged or returned in API responses |
| T-03-02 | T (Tampering) | GeminiFeedbackRequestSchema | mitigate | z.strictObject rejects unknown fields; all fields typed and bounded |
</threat_model>

<verification>
- `npx tsc --noEmit -p packages/contracts/tsconfig.json` exits 0
- `npx tsc --noEmit -p apps/api/tsconfig.json` exits 0
- `grep -c "SUPABASE" apps/api/src/config/env.ts` returns 0
- `grep "GEMINI_MODEL" apps/api/src/config/env.ts` returns the schema line
</verification>

<success_criteria>
- Contracts package exports enriched GeminiFeedbackRequest/Response types with full field typing
- API env schema is Supabase-free and includes GEMINI_MODEL
- Both packages compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-json-mode-ielts-feedback/03-01-SUMMARY.md`
</output>
