---
phase: 06-learner-dashboard-analysis-views
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/web/app/page.tsx
  - apps/web/app/page.test.tsx
  - apps/web/components/json-analysis/json-analysis-panel.tsx
  - apps/web/components/json-analysis/json-analysis-panel.test.tsx
  - apps/web/components/json-analysis/summary-metric-cards.tsx
  - apps/web/app/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "D-01: Dashboard header answers `What should I practice next?` and leads with the clearest practice priority."
    - "D-02: Dashboard priority includes one compact reason, not a long coaching report."
    - "D-03: Top-level modes remain two practice paths: JSON Analysis and Live Audio Practice."
    - "Metric strip contains exactly four primary cards: Pronunciation, Pronunciation Band, Fluency Band, WPM."
    - "Dashboard priority is derived from existing analysis outputs only."
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Top-level JSON Analysis / Live Audio Practice mode switch"
    - path: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      provides: "Learner-first dashboard priority card"
    - path: "apps/web/components/json-analysis/summary-metric-cards.tsx"
      provides: "Four-card metric strip"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      via: "JSON mode render branch"
    - from: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      to: "apps/web/components/json-analysis/summary-metric-cards.tsx"
      via: "summary prop"
---

<objective>
Implement the learner-first dashboard shell, top-level practice-path copy, priority header, and four-card metric strip.
</objective>

<tasks>
<task type="auto">
  <name>Update top-level practice-path mode switch</name>
  <files>apps/web/app/page.tsx, apps/web/app/page.test.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/app/page.tsx
    - apps/web/app/globals.css
    - apps/web/app/page.test.tsx
    - .planning/phases/06-learner-dashboard-analysis-views/06-UI-SPEC.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Follow the `apps/web/app/page.tsx` assignment in `06-PATTERNS.md`: keep the existing `Mode = "json" | "audio"` state and button-based `aria-pressed` switch. Render two mode cards/buttons with labels `JSON Analysis` and `Live Audio Practice`, helper descriptions `Import assessment data to inspect scores, pauses, words, and phonemes.` and `Record from your microphone and watch the AI coach respond live.`, and `aria-pressed={mode === ...}`. Do not add a top-level History mode. Update `apps/web/app/page.test.tsx` from Wave 1 current-state assertions to Phase 6 assertions for both labels, both helper descriptions, selected `aria-pressed` state, and the safety guard that `Gemini Live` is not shown while Deepgram code remains active. Add/adjust CSS in `globals.css` for secondary-card mode buttons, 44px minimum target height, accent active border, and horizontal scrolling on mobile.
  </action>
  <acceptance_criteria>
    - `grep -n "Live Audio Practice" apps/web/app/page.tsx` finds the new label.
    - `grep -n "Live Audio Practice" apps/web/app/page.test.tsx` finds the updated Phase 6 label assertion.
    - `grep -n "Record from your microphone and watch the AI coach respond live" apps/web/app/page.test.tsx` finds the helper-copy assertion.
    - `grep -n "Import assessment data to inspect scores, pauses, words, and phonemes" apps/web/app/page.tsx` finds helper copy.
    - `grep -n "aria-pressed" apps/web/app/page.tsx` finds selected-state support.
    - `grep -n "Gemini Live" apps/web/app/page.test.tsx` finds the no-incorrect-label guard.
    - `grep -n "History" apps/web/app/page.tsx` does not show a top-level mode label.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- app/page.test.tsx</automated>
  </verify>
  <done>
    Task is complete when `app/page.test.tsx` passes with Phase 6 label/helper/aria assertions, the Gemini Live safety guard still passes, and no top-level History mode is introduced.
  </done>
</task>

<task type="auto">
  <name>Add dashboard priority header and four-card metric strip</name>
  <files>apps/web/components/json-analysis/json-analysis-panel.tsx, apps/web/components/json-analysis/json-analysis-panel.test.tsx, apps/web/components/json-analysis/summary-metric-cards.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/json-analysis/json-analysis-panel.tsx
    - apps/web/components/json-analysis/summary-metric-cards.tsx
    - packages/contracts/src/json-analysis.ts
    - apps/web/components/json-analysis/json-analysis-panel.test.tsx
    - .planning/phases/06-learner-dashboard-analysis-views/06-CONTEXT.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-UI-SPEC.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Follow the `json-analysis-panel.tsx` and `summary-metric-cards.tsx` assignments in `06-PATTERNS.md`. Add a UI-only helper in `json-analysis-panel.tsx` named `derivePracticePriority(analysis: JsonAnalysisResponse)` that returns `{ priority: string; reason: string }`. Use existing fields in this order: repeated weak phoneme patterns, critical/longest pause, weak-word concentration, WPM/pause ratio; fallback to `Analyze a speaking attempt to see your highest-priority pronunciation or fluency issue.` and `LocalSpeak uses your word scores, weak sound patterns, pauses, and WPM to choose one focused next step.`. Render a dashboard card headed `What should I practice next?` above result tabs. Update `SummaryMetricCards` to render exactly `Pronunciation`, `Pronunciation Band`, `Fluency Band`, `WPM`; remove Pause ratio from the primary grid. Update `json-analysis-panel.test.tsx` to import the Phase 6 fixtures and assert the priority card, exact four metric labels, and absence of Pause ratio from the primary metric grid.
  </action>
  <acceptance_criteria>
    - `grep -n "derivePracticePriority" apps/web/components/json-analysis/json-analysis-panel.tsx` finds the helper.
    - `grep -n "What should I practice next" apps/web/components/json-analysis/json-analysis-panel.tsx` finds dashboard heading.
    - `grep -n "What should I practice next" apps/web/components/json-analysis/json-analysis-panel.test.tsx` finds dashboard test coverage.
    - `grep -n "Pronunciation\\|Pronunciation Band\\|Fluency Band\\|WPM" apps/web/components/json-analysis/summary-metric-cards.tsx` finds all four labels.
    - `grep -n "Pause ratio" apps/web/components/json-analysis/json-analysis-panel.test.tsx` finds an absence assertion for the primary metric grid.
    - `grep -n "Pause ratio" apps/web/components/json-analysis/summary-metric-cards.tsx` returns no primary metric label.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx</automated>
  </verify>
  <done>
    Task is complete when updated JSON analysis tests prove the dashboard priority card, exact four-card metric strip, and primary-grid Pause ratio removal.
  </done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
|-----------|----------|-----------|------------|
| T-06-03 | Information integrity | Audio mode labels | Use generic `Live Audio Practice` / `AI coach` copy unless implementation is reconciled to Gemini Live. |
</threat_model>

<verification>
<automated>pnpm --filter web check && pnpm --filter web test</automated>
</verification>

<success_criteria>
- UI-01 dashboard metrics are visible in the primary dashboard strip.
- UI-02 practice-path switching is visible and accessible.
- D-01 through D-04 are implemented.
</success_criteria>
