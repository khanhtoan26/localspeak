---
phase: 06-learner-dashboard-analysis-views
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - apps/web/components/json-analysis/result-tabs.tsx
  - apps/web/components/json-analysis/json-analysis-panel.test.tsx
  - apps/web/components/json-analysis/pauses-tab.tsx
  - apps/web/components/json-analysis/words-tab.tsx
  - apps/web/components/json-analysis/phonemes-tab.tsx
  - apps/web/app/globals.css
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05
  - UI-06
must_haves:
  truths:
    - "JSON tabs are exactly Pause Analysis, Words, Phonemes, IELTS Analysis."
    - "D-05: Pause Analysis is timeline-first and still includes sorted detail rows."
    - "D-06: Pause timeline uses color plus accessible labels and a severity legend."
    - "D-07: Pause rows show before/after words plus duration."
    - "D-08: Worst pause includes one explicit practice cue."
    - "D-09: Words render as sentence-order chips, not only weak-word rows."
    - "D-10: Weak-word shortlist is secondary and only appears when useful."
    - "D-11: Phonemes render impact-ranked bars using score, repeat count, and supported learner relevance."
    - "D-12: Phoneme rows include sound labels, examples, and learner explanations."
    - "D-13: Vietnamese hints appear only when analysis data supports known patterns."
  artifacts:
    - path: "apps/web/components/json-analysis/result-tabs.tsx"
      provides: "Four-tab JSON analysis navigation"
    - path: "apps/web/components/json-analysis/pauses-tab.tsx"
      provides: "Pause summary, SVG timeline, cue, and sorted list"
    - path: "apps/web/components/json-analysis/words-tab.tsx"
      provides: "Sentence-order word chips and weak shortlist"
    - path: "apps/web/components/json-analysis/phonemes-tab.tsx"
      provides: "Impact-ranked phoneme bars and hints"
  key_links:
    - from: "apps/web/components/json-analysis/result-tabs.tsx"
      to: "PausesTab|WordsTab|PhonemesTab|AiCoachTab"
      via: "active tab render branches"
    - from: "apps/web/components/json-analysis/pauses-tab.tsx"
      to: "PauseMetric"
      via: "pauses prop"
    - from: "apps/web/components/json-analysis/words-tab.tsx"
      to: "WordMetric.word.band"
      via: "word chip class and label"
    - from: "apps/web/components/json-analysis/phonemes-tab.tsx"
      to: "WeakPhonemePattern"
      via: "impact ranking"
---

<objective>
Implement the Phase 6 JSON analysis tabs and learner-friendly pause, word, and phoneme visualizations.
</objective>

<tasks>
<task type="auto">
  <name>Refactor JSON result tabs to Phase 6 contract</name>
  <files>apps/web/components/json-analysis/result-tabs.tsx, apps/web/components/json-analysis/json-analysis-panel.test.tsx</files>
  <read_first>
    - apps/web/components/json-analysis/result-tabs.tsx
    - apps/web/components/json-analysis/ai-coach-tab.tsx
    - apps/web/components/json-analysis/json-analysis-panel.test.tsx
    - .planning/phases/06-learner-dashboard-analysis-views/06-UI-SPEC.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Change `tabs` to `["Pause Analysis", "Words", "Phonemes", "IELTS Analysis"] as const`. Set default active tab to `"Pause Analysis"`. Remove `SummaryTab` and its render branch. Render `PausesTab` for `Pause Analysis` and `AiCoachTab` for `IELTS Analysis`. Keep `aria-pressed` selected state and horizontal-scroll-compatible class names. Update `json-analysis-panel.test.tsx` to assert the exact four tab labels, default Pause Analysis selection, IELTS Analysis branch, and no Summary tab.
  </action>
  <acceptance_criteria>
    - `grep -n "Pause Analysis" apps/web/components/json-analysis/result-tabs.tsx` finds the first tab.
    - `grep -n "IELTS Analysis" apps/web/components/json-analysis/result-tabs.tsx` finds the AI tab.
    - `grep -n "Summary" apps/web/components/json-analysis/result-tabs.tsx` returns no tab or branch.
    - `grep -n "useState<TabName>(\"Pause Analysis\")" apps/web/components/json-analysis/result-tabs.tsx` finds the default.
    - `grep -n "Pause Analysis" apps/web/components/json-analysis/json-analysis-panel.test.tsx` finds tab-contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx</automated>
  </verify>
  <done>
    Task is complete when updated tests pass with exactly four tabs, default Pause Analysis selection, IELTS Analysis branch, and no Summary tab branch.
  </done>
</task>

<task type="auto">
  <name>Implement pause summary, SVG timeline, legend, cue, and sorted list</name>
  <files>apps/web/components/json-analysis/pauses-tab.tsx, apps/web/components/json-analysis/json-analysis-panel.test.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/json-analysis/pauses-tab.tsx
    - packages/contracts/src/json-analysis.ts
    - apps/web/components/json-analysis/json-analysis-panel.test.tsx
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Update `PausesTab` to accept `pauses: PauseMetric[]` and enough summary data if needed from parent; otherwise derive total pause time, critical count, and longest duration from pauses. Render sections in this order: pause summary card, accessible SVG timeline with title/description and pause segment labels, severity legend, worst pause cue headed `Practice this pause first`, sorted list by `duration` descending. Each row must include `{Severity} pause`, `{duration}s between "{beforeWord}" and "{afterWord}"`, and `gap: {startTime}s–{endTime}s`. Update `json-analysis-panel.test.tsx` pause coverage to use Phase 6 fixtures and assert the timeline, summary, legend, worst-pause cue, before/after words, duration-sorted rows, and accessible timeline name/description.
  </action>
  <acceptance_criteria>
    - `grep -n "Practice this pause first" apps/web/components/json-analysis/pauses-tab.tsx` finds cue heading.
    - `grep -n "<svg" apps/web/components/json-analysis/pauses-tab.tsx` finds timeline rendering.
    - `grep -n "sort" apps/web/components/json-analysis/pauses-tab.tsx` finds duration sorting.
    - `grep -n "between" apps/web/components/json-analysis/pauses-tab.tsx` finds before/after copy.
    - `grep -n "Practice this pause first" apps/web/components/json-analysis/json-analysis-panel.test.tsx` finds pause cue test coverage.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx -t "pause"</automated>
  </verify>
  <done>
    Task is complete when updated Pause Analysis tests prove summary, SVG timeline, legend, worst-pause cue, before/after row copy, and duration-sorted rows.
  </done>
</task>

<task type="auto">
  <name>Implement sentence-order word chips and weak shortlist</name>
  <files>apps/web/components/json-analysis/words-tab.tsx, apps/web/components/json-analysis/json-analysis-panel.test.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/json-analysis/words-tab.tsx
    - apps/web/components/json-analysis/json-analysis-panel.test.tsx
    - packages/contracts/src/json-analysis.ts
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Replace list rows with sentence-order chips for every `WordMetric` in original order. Each chip shows word and score percent, uses existing `word.band` for class/color, and has accessible label `{word}, {band}, {scorePercent} percent, from {startTime}s to {endTime}s`. Add legend for Good/Okay/Weak. Add secondary weak shortlist headed `Weak words to repeat`, sorted by lowest score, limited to 5, shown only when weak words exist. Do not return the old empty state when all words are okay/good. Update `json-analysis-panel.test.tsx` word coverage to assert sentence-order chips for all words, band-based accessible labels/classes, Good/Okay/Weak legend, and secondary weak shortlist sorting/limit.
  </action>
  <acceptance_criteria>
    - `grep -n "Weak words to repeat" apps/web/components/json-analysis/words-tab.tsx` finds shortlist heading.
    - `grep -n "aria-label" apps/web/components/json-analysis/words-tab.tsx` finds chip accessible labels.
    - `grep -n "word.band" apps/web/components/json-analysis/words-tab.tsx` finds contract band usage.
    - `grep -n "Weak words to repeat" apps/web/components/json-analysis/json-analysis-panel.test.tsx` finds word-chip test coverage.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx -t "word"</automated>
  </verify>
  <done>
    Task is complete when updated Words tests prove all sentence-order chips, band-derived labels/classes, legend, and secondary weak shortlist behavior.
  </done>
</task>

<task type="auto">
  <name>Implement phoneme impact rows with bars and conditional Vietnamese hints</name>
  <files>apps/web/components/json-analysis/phonemes-tab.tsx, apps/web/components/json-analysis/json-analysis-panel.test.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/json-analysis/phonemes-tab.tsx
    - apps/web/components/json-analysis/json-analysis-panel.test.tsx
    - packages/contracts/src/json-analysis.ts
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Add an impact-ranking helper that combines lower `averageScore`, higher `weakOccurrenceCount`, and supported Vietnamese relevance when ARPAbet/IPA/example words indicate `/θ/`, final consonants, or clusters `thr-`, `str-`, `tr-`. Render rows with ARPAbet, IPA examples, weak occurrence count, average score percent, example words, horizontal weakness bar, and one explanation sentence. Show Vietnamese hints only when the detected pattern supports them; otherwise omit hint copy. Update `json-analysis-panel.test.tsx` phoneme coverage to assert impact ordering, bar rendering, occurrence/score metadata, explanation copy, and conditional Vietnamese hints only for data-supported patterns.
  </action>
  <acceptance_criteria>
    - `grep -n "weakOccurrenceCount" apps/web/components/json-analysis/phonemes-tab.tsx` finds row meta.
    - `grep -n "averageScore" apps/web/components/json-analysis/phonemes-tab.tsx` finds score rendering/ranking.
    - `grep -n "Vietnamese speakers often replace /θ/" apps/web/components/json-analysis/phonemes-tab.tsx` finds conditional hint copy.
    - `grep -n "phoneme-bar" apps/web/components/json-analysis/phonemes-tab.tsx` or equivalent CSS class finds bar rendering.
    - `grep -n "Vietnamese speakers often replace /θ/" apps/web/components/json-analysis/json-analysis-panel.test.tsx` finds conditional hint test coverage.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx -t "phoneme"</automated>
  </verify>
  <done>
    Task is complete when updated phoneme tests prove impact ranking, bars, explanations, occurrence/score metadata, and data-supported Vietnamese hints only.
  </done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
|-----------|----------|-----------|------------|
| T-06-04 | XSS | Word/pause/phoneme text rendering | Use React text rendering only; do not use `dangerouslySetInnerHTML`. |
| T-06-05 | Information integrity | Vietnamese hints | Render hints only for supported detected patterns. |
</threat_model>

<verification>
<automated>pnpm --filter web check && pnpm --filter web test</automated>
</verification>

<success_criteria>
- UI-03 through UI-06 are implemented.
- D-05 through D-13 are implemented.
- Visualizations are accessible and test-covered.
</success_criteria>
