---
phase: 06-learner-dashboard-analysis-views
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - apps/web/components/json-analysis/json-analysis-panel.tsx
  - apps/web/components/json-analysis/ai-coach-tab.tsx
  - apps/web/components/json-analysis/saved-sessions-panel.tsx
  - apps/web/components/json-analysis/saved-sessions-panel.test.tsx
  - apps/web/lib/saved-sessions/owner-key.ts
  - apps/web/app/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-07
must_haves:
  truths:
    - "D-04: Saved-session controls remain secondary and do not distract from the current result."
    - "D-14: AI coaching remains opt-in and deterministic results stay visible."
    - "D-15: JSON-mode AI feedback uses existing strict JSON endpoint with polished loading states, not a required backend streaming change."
    - "D-17: AI Coach states cover idle, loading, success, error, and retry without hiding deterministic results."
    - "D-18: Phase 6 includes a light saved-session UX."
    - "D-19: Saved-session UI supports explicit save, history list, and reopen only."
    - "D-20: Saved-session UI is secondary and ownerKey-scoped, not a top-level mode."
    - "D-21: Saved-session scope excludes rename, delete, account linking, authentication, and advanced analytics."
  artifacts:
    - path: "apps/web/components/json-analysis/ai-coach-tab.tsx"
      provides: "IELTS Analysis idle/loading/success/error/retry states"
    - path: "apps/web/components/json-analysis/saved-sessions-panel.tsx"
      provides: "Secondary save/history/reopen UI"
    - path: "apps/web/components/json-analysis/saved-sessions-panel.test.tsx"
      provides: "OwnerKey, save/list/reopen, Zod parsing, and sanitization coverage"
    - path: "apps/web/lib/saved-sessions/owner-key.ts"
      provides: "Browser-local ownerKey partitioning"
  key_links:
    - from: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      to: "apps/web/components/json-analysis/ai-coach-tab.tsx"
      via: "aiCoachState prop"
    - from: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      to: "apps/web/components/json-analysis/saved-sessions-panel.tsx"
      via: "current analysis and reopen callback"
    - from: "apps/web/components/json-analysis/saved-sessions-panel.tsx"
      to: "/api/saved-sessions"
      via: "fetch with SavedSession*ResponseSchema parsing"
    - from: "apps/web/components/json-analysis/saved-sessions-panel.tsx"
      to: "apps/web/lib/saved-sessions/owner-key.ts"
      via: "getOrCreateOwnerKey()"
---

<objective>
Polish IELTS Analysis states and implement lightweight saved-session save/history/reopen UI.
</objective>

<tasks>
<task type="auto">
  <name>Polish IELTS Analysis AI Coach states</name>
  <files>apps/web/components/json-analysis/ai-coach-tab.tsx, apps/web/components/json-analysis/json-analysis-panel.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/json-analysis/ai-coach-tab.tsx
    - apps/web/components/json-analysis/json-analysis-panel.tsx
    - packages/contracts/src/gemini-feedback.ts
    - .planning/phases/06-learner-dashboard-analysis-views/06-UI-SPEC.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Update user-facing AI copy to UI-SPEC values: idle `Get AI feedback after reviewing the deterministic metrics.`, loading `Generating personalized IELTS feedback…`, error `AI feedback unavailable. Your deterministic results are still available — try again when you're ready.`, retry CTA `Retry AI Feedback`. Keep current strict JSON `/api/gemini-feedback` behavior. Ensure dashboard and tabs remain mounted while `aiCoachState.status` is `loading` or `error`.
  </action>
  <acceptance_criteria>
    - `grep -n "Get AI feedback after reviewing the deterministic metrics" apps/web/components/json-analysis/ai-coach-tab.tsx` finds idle copy.
    - `grep -n "Generating personalized IELTS feedback" apps/web/components/json-analysis/ai-coach-tab.tsx` finds loading copy.
    - `grep -n "Retry AI Feedback" apps/web/components/json-analysis/ai-coach-tab.tsx` finds retry CTA.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx -t "AI feedback"</automated>
  </verify>
  <done>
    Task is complete when AI feedback tests pass and deterministic dashboard/tabs remain mounted during AI loading and error states.
  </done>
</task>

<task type="auto">
  <name>Add ownerKey utility and saved-session panel</name>
  <files>apps/web/lib/saved-sessions/owner-key.ts, apps/web/components/json-analysis/saved-sessions-panel.tsx, apps/web/components/json-analysis/saved-sessions-panel.test.tsx, apps/web/components/json-analysis/json-analysis-panel.tsx, apps/web/app/globals.css</files>
  <read_first>
    - packages/contracts/src/saved-session.ts
    - apps/api/src/saved-sessions/saved-sessions.controller.ts
    - apps/web/components/json-analysis/json-analysis-panel.tsx
    - apps/web/components/json-analysis/test-fixtures.ts
    - .planning/phases/06-learner-dashboard-analysis-views/06-CONTEXT.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-RESEARCH.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Create `apps/web/lib/saved-sessions/owner-key.ts` exporting `getOrCreateOwnerKey(): string` that reads `localspeak.ownerKey.v1` from `localStorage`, creates `crypto.randomUUID()` if missing, persists it, and treats it only as local-history partitioning. Create `SavedSessionsPanel` with secondary UI for `Save Result`, history list, and `Reopen Result`. Use `SavedSessionCreateResponseSchema`, `SavedSessionListResponseSchema`, and `SavedSessionDetailResponseSchema` to parse responses from `/api/saved-sessions`. Save requests must include `ownerKey`, `inputMode: "json"`, `referenceText`, sanitized `inputMetadata`, `metrics` from the derived analysis response, and optional `feedback`; never include `speechAssessment`, `rawSpeechAssessment`, or `vendorPayload`. Reopen must restore saved metrics snapshot without recomputing analysis and show marker `Reopened saved result`. Create/update `saved-sessions-panel.test.tsx` using Phase 6 fixtures to cover ownerKey creation/reuse, save request body shape, list response parsing, detail/reopen flow, Zod parse failures surfacing as errors, and raw vendor payload exclusion.
  </action>
  <acceptance_criteria>
    - `grep -n "localspeak.ownerKey.v1" apps/web/lib/saved-sessions/owner-key.ts` finds storage key.
    - `grep -n "Save Result" apps/web/components/json-analysis/saved-sessions-panel.tsx` finds save CTA.
    - `grep -n "Reopen Result" apps/web/components/json-analysis/saved-sessions-panel.tsx` finds reopen CTA.
    - `grep -n "SavedSessionListResponseSchema" apps/web/components/json-analysis/saved-sessions-panel.tsx` finds list parsing.
    - `grep -n "localspeak.ownerKey.v1" apps/web/components/json-analysis/saved-sessions-panel.test.tsx` finds ownerKey test coverage.
    - `grep -n "vendorPayload" apps/web/components/json-analysis/saved-sessions-panel.test.tsx` finds exclusion test coverage.
    - `grep -n "speechAssessment\\|rawSpeechAssessment\\|vendorPayload" apps/web/components/json-analysis/saved-sessions-panel.tsx` returns no request construction usage except explicit exclusion tests/comments if needed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- components/json-analysis/saved-sessions-panel.test.tsx components/json-analysis/json-analysis-panel.test.tsx</automated>
  </verify>
  <done>
    Task is complete when `saved-sessions-panel.test.tsx` is created/updated and passes for ownerKey creation/reuse, save/list/reopen, Zod response parsing/failure surfacing, and sanitized request bodies.
  </done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
|-----------|----------|-----------|------------|
| T-06-01 | Reliability / integrity | AI feedback UI | AI loading/error states are scoped to IELTS Analysis; deterministic results remain visible. |
| T-06-02 | Information disclosure / spoofing | Saved-session UI | ownerKey scopes create/list/fetch; raw vendor payload keys are excluded. |
</threat_model>

<verification>
<automated>pnpm --filter web check && pnpm --filter web test</automated>
</verification>

<success_criteria>
- UI-07 is polished and remains compatible with existing backend.
- D-14 through D-21 are implemented.
- Saved-session UI is lightweight, secondary, tested, and sanitized.
</success_criteria>
