---
phase: 06-learner-dashboard-analysis-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.test.tsx
  - apps/web/components/json-analysis/test-fixtures.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
must_haves:
  truths:
    - "Wave 1 creates only passing test scaffolding and guards; future-facing assertions live with implementation tasks."
    - "Tests guard against labeling the current audio implementation as Gemini Live while Deepgram code remains active."
    - "Shared test fixtures are ready for later dashboard, visualization, AI, and saved-session tests."
  artifacts:
    - path: "apps/web/app/page.test.tsx"
      provides: "Passing top-level mode smoke tests and audio label safety guard"
    - path: "apps/web/components/json-analysis/test-fixtures.ts"
      provides: "Reusable JsonAnalysisResponse and GeminiFeedbackResponse fixtures for implementation-plan tests"
  key_links:
    - from: "apps/web/app/page.test.tsx"
      to: "apps/web/app/page.tsx"
      via: "rendered mode-switch assertions"
    - from: "apps/web/components/json-analysis/test-fixtures.ts"
      to: "packages/contracts/src/json-analysis.ts"
      via: "fixture shape matches JsonAnalysisResponse"
---

<objective>
Create passing Phase 6 test scaffolding and safety guards before changing production UI.
</objective>

<tasks>
<task type="auto">
  <name>Add top-level mode smoke tests and audio label guard</name>
  <files>apps/web/app/page.test.tsx</files>
  <read_first>
    - apps/web/app/page.tsx
    - apps/web/components/audio-mode/audio-mode-panel.tsx
    - apps/web/components/audio-mode/use-deepgram-session.ts
    - .planning/phases/06-learner-dashboard-analysis-views/06-CONTEXT.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-UI-SPEC.md
    - .planning/phases/06-learner-dashboard-analysis-views/06-VALIDATION.md
  </read_first>
  <action>
    Create `apps/web/app/page.test.tsx` with Vitest and Testing Library using expectations that pass against the current page before Phase 6 implementation. Assert the page renders the current JSON mode label `JSON Analysis`, renders the current audio label `Live Audio`, exposes selected state via `aria-pressed`, and does not render `Gemini Live` while `AudioModePanel` still imports `useDeepgramSession`. Do not assert future Phase 6 helper copy or `Live Audio Practice` in this Wave 1 plan; those assertions belong in Plan 06-02 with the implementation that makes them pass.
  </action>
  <acceptance_criteria>
    - `grep -n "JSON Analysis" apps/web/app/page.test.tsx` finds a visible-label assertion.
    - `grep -n "Live Audio" apps/web/app/page.test.tsx` finds a current-label assertion.
    - `grep -n "Gemini Live" apps/web/app/page.test.tsx` finds a guard assertion.
    - `grep -n "Live Audio Practice" apps/web/app/page.test.tsx` returns no future-label assertion.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test -- app/page.test.tsx</automated>
  </verify>
  <done>
    Task is complete when the new page test file passes against the current UI and protects against incorrect `Gemini Live` copy before audio implementation reconciliation.
  </done>
</task>

<task type="auto">
  <name>Create reusable Phase 6 JSON analysis test fixtures</name>
  <files>apps/web/components/json-analysis/test-fixtures.ts</files>
  <read_first>
    - apps/web/components/json-analysis/json-analysis-panel.test.tsx
    - packages/contracts/src/json-analysis.ts
    - packages/contracts/src/gemini-feedback.ts
    - packages/contracts/src/saved-session.ts
    - .planning/phases/06-learner-dashboard-analysis-views/06-PATTERNS.md
  </read_first>
  <action>
    Create `apps/web/components/json-analysis/test-fixtures.ts` exporting reusable valid fixture builders for later implementation-plan tests: `createJsonAnalysisResponseFixture()`, `createGeminiFeedbackFixture()`, and `createSavedSessionFixture()`. The JSON fixture must include summary metrics, weak phoneme patterns, at least three words with good/okay/weak bands, at least two pauses with different durations, and no raw vendor keys. Do not add future-facing assertions in this task; implementation plans 06-02, 06-03, and 06-04 will import these fixtures when adding tests that pass with their production changes.
  </action>
  <acceptance_criteria>
    - `grep -n "createJsonAnalysisResponseFixture" apps/web/components/json-analysis/test-fixtures.ts` finds the JSON fixture builder.
    - `grep -n "createGeminiFeedbackFixture" apps/web/components/json-analysis/test-fixtures.ts` finds the feedback fixture builder.
    - `grep -n "createSavedSessionFixture" apps/web/components/json-analysis/test-fixtures.ts` finds the saved-session fixture builder.
    - `grep -n "vendorPayload" apps/web/components/json-analysis/test-fixtures.ts` returns no fixture payload key.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web check && pnpm --filter web test -- components/json-analysis/json-analysis-panel.test.tsx</automated>
  </verify>
  <done>
    Task is complete when shared fixtures compile via web typecheck, existing JSON analysis tests still pass, and no future Phase 6 behavior assertions have been introduced before implementation.
  </done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
|-----------|----------|-----------|------------|
| T-06-01 | Information integrity | AI feedback UI | Tests ensure AI failure affects only IELTS Analysis and deterministic views remain visible. |
| T-06-02 | Information disclosure | Saved-session UI | Tests ensure ownerKey scoping and reject raw vendor payload keys. |
| T-06-03 | Information integrity | Audio mode labels | Tests prevent `Gemini Live` claims while Deepgram implementation is active. |
</threat_model>

<verification>
<automated>pnpm --filter web test -- app/page.test.tsx components/json-analysis/json-analysis-panel.test.tsx</automated>
</verification>

<success_criteria>
- Passing test scaffolding exists before implementation plans.
- Audio label mismatch guard is encoded before implementation.
- Reusable fixtures are ready for UI-01 through UI-07 tests in later plans.
</success_criteria>
