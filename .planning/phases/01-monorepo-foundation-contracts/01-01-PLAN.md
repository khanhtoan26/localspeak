---
phase: 01-monorepo-foundation-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .gitignore
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/vitest.config.mts
  - packages/contracts/src/index.ts
  - packages/contracts/src/speech-assessment.ts
  - packages/contracts/src/json-analysis.ts
  - packages/contracts/src/audio-analysis.ts
  - packages/contracts/src/saved-session.ts
  - packages/contracts/src/gemini-feedback.ts
  - packages/contracts/test/speech-assessment.fixture.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "D-01: Use a pnpm workspace."
    - "D-02: Use top-level layout apps/web, apps/api, and packages/contracts."
    - "D-03: Root scripts include dev, dev:web, dev:api, check, test, and build."
    - "D-05: Shared API contracts use Zod schemas with inferred TypeScript types."
    - "D-06: Contract shells exist for JSON analysis, audio analysis, saved sessions, and Gemini feedback."
    - "D-07: Speech assessment JSON validates required known fields while passing through unknown vendor fields."
    - "D-08: .artifacts/speech-response.json is used as a contract validation fixture."
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace globs for apps/* and packages/*"
      contains: "packages/*"
    - path: "packages/contracts/src/speech-assessment.ts"
      provides: "SpeechAssessmentResponseSchema for vendor speech fixture"
      contains: "SpeechAssessmentResponseSchema"
    - path: "packages/contracts/test/speech-assessment.fixture.test.ts"
      provides: "Fixture validation and unknown vendor passthrough tests"
      contains: "vendor_extra"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "apps/web/package.json"
      via: "workspace layout enables the Next.js app package"
    - from: "pnpm-workspace.yaml"
      to: "apps/api/package.json"
      via: "workspace layout enables the NestJS API package"
    - from: "packages/contracts/src/index.ts"
      to: "apps/api/src/contracts/contracts.controller.ts"
      via: "API imports shared Zod schemas from @localspeak/contracts"
    - from: "packages/contracts/src/index.ts"
      to: "apps/web/next.config.ts"
      via: "web app transpiles shared workspace contracts"
---

<objective>
Create the root pnpm workspace and shared contracts package that all later Phase 1 plans depend on.

This plan establishes the monorepo layout and `@localspeak/contracts` package before the NestJS API and Next.js web app consume shared schemas.
</objective>

<context>
@copilot-instructions.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-monorepo-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation-contracts/01-RESEARCH.md
@.planning/phases/01-monorepo-foundation-contracts/01-VALIDATION.md
@.planning/phases/01-monorepo-foundation-contracts/01-PATTERNS.md
@.artifacts/speech-response.json
</context>

<tasks>

<task type="auto">
  <name>Create pnpm workspace root</name>
  <read_first>
    - `copilot-instructions.md`
    - `.planning/phases/01-monorepo-foundation-contracts/01-CONTEXT.md`
    - `.planning/phases/01-monorepo-foundation-contracts/01-RESEARCH.md`
    - `package.json` (create if missing)
    - `pnpm-workspace.yaml` (create if missing)
    - `tsconfig.base.json` (create if missing)
    - `.gitignore` (create if missing)
  </read_first>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, .gitignore</files>
  <action>Create root `package.json` with `"private": true`, `"packageManager": "pnpm@10.33.0"`, dev dependency `concurrently`, and scripts exactly named `dev`, `dev:web`, `dev:api`, `check`, `test`, and `build`. `dev` must run web and API concurrently. `dev:web` must run `pnpm --filter web dev`; `dev:api` must run `pnpm --filter api start:dev`; `check`, `test`, and `build` must use recursive workspace commands. Create `pnpm-workspace.yaml` with package globs `apps/*` and `packages/*`. Create `tsconfig.base.json` with strict TypeScript defaults and `resolveJsonModule: true`. Create `.gitignore` for `node_modules`, `.next`, `dist`, `coverage`, `.env`, `.env.local`, and app-local env files, while explicitly allowing `.env.example` files.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pkg=require('./package.json'); const ws=fs.readFileSync('pnpm-workspace.yaml','utf8'); for (const s of ['dev','dev:web','dev:api','check','test','build']) if (!pkg.scripts?.[s]) throw new Error('missing script '+s); if (!ws.includes('apps/*') || !ws.includes('packages/*')) throw new Error('workspace globs missing');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'apps/\*' pnpm-workspace.yaml`
    - `grep -q 'packages/\*' pnpm-workspace.yaml`
    - `grep -q 'concurrently' package.json`
    - `grep -q 'resolveJsonModule' tsconfig.base.json`
    - `grep -q '!.env.example' .gitignore`
  </acceptance_criteria>
  <done>
    - Root `package.json` contains `dev`, `dev:web`, `dev:api`, `check`, `test`, and `build`.
    - `pnpm-workspace.yaml` includes `apps/*` and `packages/*`.
    - `tsconfig.base.json` enables strict TypeScript and JSON imports.
    - `.gitignore` excludes local env files while preserving `.env.example`.
  </done>
</task>

<task type="auto">
  <name>Create shared Zod contracts package</name>
  <read_first>
    - `.planning/phases/01-monorepo-foundation-contracts/01-CONTEXT.md`
    - `.planning/phases/01-monorepo-foundation-contracts/01-RESEARCH.md`
    - `.planning/phases/01-monorepo-foundation-contracts/01-PATTERNS.md`
    - `.artifacts/speech-response.json`
    - `packages/contracts/package.json` (create if missing)
    - `packages/contracts/src/index.ts` (create if missing)
    - `packages/contracts/src/speech-assessment.ts` (create if missing)
  </read_first>
  <files>packages/contracts/package.json, packages/contracts/tsconfig.json, packages/contracts/vitest.config.mts, packages/contracts/src/index.ts, packages/contracts/src/speech-assessment.ts, packages/contracts/src/json-analysis.ts, packages/contracts/src/audio-analysis.ts, packages/contracts/src/saved-session.ts, packages/contracts/src/gemini-feedback.ts, packages/contracts/test/speech-assessment.fixture.test.ts</files>
  <action>Create package `@localspeak/contracts` with Zod and TypeScript build/test scripts. Implement `SpeechPhoneSchema`, `SpeechWordSchema`, and `SpeechAssessmentResponseSchema` using `z.looseObject` so unknown vendor fields pass through. Required fixture fields must include `success`, `msg`, `result`, `text_refs`, `audio_url`, `total_score`, and `response_time`. Because `.artifacts/speech-response.json` contains `"response_time": "1.711"`, the schema must accept the string value, either with `z.string()` or a documented coercing/union schema. Add shell schemas/types named `JsonAnalysisRequestSchema`, `JsonAnalysisResponseSchema`, `AudioAnalysisRequestSchema`, `AudioAnalysisResponseSchema`, `SavedAnalysisSessionSchema`, `SavedSessionCreateRequestSchema`, `GeminiFeedbackRequestSchema`, and `GeminiFeedbackResponseSchema`. Export all contract files from `src/index.ts`. Add a Vitest fixture test that imports `.artifacts/speech-response.json`, validates it, asserts `response_time` handling, and asserts `vendor_extra` survives parsing.</action>
  <verify>
    <automated>pnpm --filter @localspeak/contracts test</automated>
    <automated>pnpm --filter @localspeak/contracts check</automated>
    <automated>pnpm --filter @localspeak/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'z.looseObject' packages/contracts/src/speech-assessment.ts`
    - `grep -q 'response_time' packages/contracts/src/speech-assessment.ts`
    - `grep -q 'vendor_extra' packages/contracts/test/speech-assessment.fixture.test.ts`
    - `grep -q 'JsonAnalysisRequestSchema' packages/contracts/src/json-analysis.ts`
    - `grep -q 'AudioAnalysisRequestSchema' packages/contracts/src/audio-analysis.ts`
    - `grep -q 'SavedAnalysisSessionSchema' packages/contracts/src/saved-session.ts`
    - `grep -q 'GeminiFeedbackResponseSchema' packages/contracts/src/gemini-feedback.ts`
    - `pnpm --filter @localspeak/contracts test` exits 0
  </acceptance_criteria>
  <done>
    - `@localspeak/contracts` builds and exports all shared Zod schemas and inferred types.
    - `SpeechAssessmentResponseSchema` validates `.artifacts/speech-response.json`.
    - Fixture tests prove unknown vendor fields survive parsing.
    - Contract shell schemas exist for JSON analysis, audio analysis, saved sessions, and Gemini feedback.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Vendor fixture to contracts | Untrusted speech assessment JSON becomes trusted app types after validation. |
| Contracts package to apps | Runtime schemas and inferred types are shared by web and API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Information Disclosure | contracts package | mitigate | Do not define `GEMINI_API_KEY`, `SUPABASE_SECRET_KEY`, or `NEXT_PUBLIC_GEMINI_*` in contracts. |
| T-01-02 | Information Disclosure | frontend dependency graph | mitigate | Shared contracts must not require server secret values. |
| T-01-03 | Tampering | `SpeechAssessmentResponseSchema` | mitigate | Validate required known fields with Zod and preserve unknown vendor fields with `z.looseObject`. |
| T-01-04 | Information Disclosure | future health models | transfer | API plan owns safe health response; contracts package must not include secret-bearing health fields. |
| T-01-05 | Information Disclosure | workspace setup | transfer | Web/API plans use same-origin rewrite; this plan does not enable CORS. |
</threat_model>

<verification>
- `pnpm --filter @localspeak/contracts test`
- `pnpm --filter @localspeak/contracts check`
- `pnpm --filter @localspeak/contracts build`
- `pnpm check`
</verification>

<success_criteria>
- Root pnpm workspace exists with required scripts and workspace globs.
- `@localspeak/contracts` exists and builds.
- Speech fixture validates and unknown vendor fields pass through.
- Contract shells exist for all v1 top-level domains.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation-contracts/01-01-SUMMARY.md`.
</output>
