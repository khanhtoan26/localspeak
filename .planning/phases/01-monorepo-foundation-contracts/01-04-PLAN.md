---
phase: 01-monorepo-foundation-contracts
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - README.md
  - package.json
  - apps/api/.env.example
  - apps/web/.env.example
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-04
must_haves:
  truths:
    - "D-03: README documents root dev, dev:web, and dev:api commands."
    - "D-09: README documents hosted Supabase env vars and defers local Supabase CLI."
    - "D-11: Root docs and app-specific .env.example files describe required setup."
    - "D-16: Final verification runs pnpm check, pnpm test, and pnpm build."
  artifacts:
    - path: "README.md"
      provides: "Local setup and verification documentation"
      contains: "pnpm dev"
  key_links:
    - from: "package.json"
      to: "README.md"
      via: "README documents actual root workspace commands"
    - from: "apps/api/.env.example"
      to: "README.md"
      via: "README documents backend-only Gemini and Supabase secret env vars"
    - from: "apps/web/.env.example"
      to: "README.md"
      via: "README documents frontend API rewrite env var"
    - from: "pnpm-workspace.yaml"
      to: "README.md"
      via: "README documents apps/web, apps/api, and packages/contracts layout"
---

<objective>
Document local setup and run final Phase 1 verification across the workspace.
</objective>

<context>
@copilot-instructions.md
@.planning/phases/01-monorepo-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation-contracts/01-RESEARCH.md
@.planning/phases/01-monorepo-foundation-contracts/01-VALIDATION.md
@.planning/phases/01-monorepo-foundation-contracts/01-UI-SPEC.md
@.planning/phases/01-monorepo-foundation-contracts/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Write setup docs and verify env examples</name>
  <read_first>
    - `.planning/phases/01-monorepo-foundation-contracts/01-CONTEXT.md`
    - `.planning/phases/01-monorepo-foundation-contracts/01-RESEARCH.md`
    - `README.md` (create if missing)
    - `apps/api/.env.example`
    - `apps/web/.env.example`
  </read_first>
  <files>README.md, apps/api/.env.example, apps/web/.env.example</files>
  <action>Create or update `README.md` with project name `LocalSpeak`, workspace layout `apps/web`, `apps/api`, `packages/contracts`, setup command `pnpm install`, dev command `pnpm dev`, separate commands `pnpm dev:web` and `pnpm dev:api`, and verification commands `pnpm check`, `pnpm test`, and `pnpm build`. Document that Phase 1 uses hosted Supabase env vars only and local Supabase CLI setup is deferred. Document `apps/api/.env.example` variables `PORT=3001`, `GEMINI_API_KEY=`, `SUPABASE_URL=`, and `SUPABASE_SECRET_KEY=`. Document `apps/web/.env.example` variable `API_INTERNAL_URL=http://localhost:3001`. State clearly that Gemini and Supabase secret keys are backend-only and must not be prefixed with `NEXT_PUBLIC_`.</action>
  <verify>
    <automated>grep -q 'pnpm dev' README.md</automated>
    <automated>grep -q 'GEMINI_API_KEY' README.md</automated>
    <automated>grep -q 'API_INTERNAL_URL=http://localhost:3001' apps/web/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'LocalSpeak' README.md`
    - `grep -q 'apps/web' README.md`
    - `grep -q 'apps/api' README.md`
    - `grep -q 'packages/contracts' README.md`
    - `grep -q 'pnpm dev:web' README.md`
    - `grep -q 'pnpm dev:api' README.md`
    - `grep -q 'local Supabase CLI setup is deferred' README.md`
    - `! grep -R 'NEXT_PUBLIC_GEMINI' README.md apps/web/.env.example`
  </acceptance_criteria>
  <done>
    - README documents LocalSpeak layout, setup, dev commands, and verification commands.
    - README documents hosted Supabase env setup and says local Supabase CLI setup is deferred.
    - API and web `.env.example` files match documented variables.
    - Documentation keeps Gemini and Supabase secret values backend-only.
  </done>
</task>

<task type="auto">
  <name>Run final workspace verification and adjust scripts if needed</name>
  <read_first>
    - `package.json`
    - `pnpm-workspace.yaml`
    - `README.md`
    - `apps/api/package.json`
    - `apps/web/package.json`
    - `packages/contracts/package.json`
  </read_first>
  <files>package.json, README.md, apps/api/package.json, apps/web/package.json, packages/contracts/package.json</files>
  <action>Run `pnpm check`, `pnpm test`, and `pnpm build`. If a root recursive script fails because a package lacks a script, add the missing script to that package rather than weakening the root command. Keep root `dev`, `dev:web`, `dev:api`, `check`, `test`, and `build` intact. Confirm the final README documents the same commands that actually exist in root `package.json`.</action>
  <verify>
    <automated>pnpm check</automated>
    <automated>pnpm test</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0
    - `pnpm test` exits 0
    - `pnpm build` exits 0
    - `node -e "const p=require('./package.json'); ['dev','dev:web','dev:api','check','test','build'].forEach(s=>{if(!p.scripts[s]) throw new Error(s)})"` exits 0
  </acceptance_criteria>
  <done>
    - Root `pnpm check`, `pnpm test`, and `pnpm build` pass.
    - Any missing package-level scripts required by root recursion are added to the owning package.
    - Root command names remain unchanged.
    - README command documentation matches root `package.json`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation to developer setup | README and env examples influence how secrets are configured. |
| Root scripts to packages | Recursive commands execute package scripts across the workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Information Disclosure | README/env docs | mitigate | Docs say Gemini API key is backend-only and never `NEXT_PUBLIC_*`. |
| T-01-02 | Information Disclosure / Elevation of Privilege | Supabase docs | mitigate | Docs keep `SUPABASE_SECRET_KEY` in `apps/api` only. |
| T-01-03 | Tampering | validation docs | mitigate | README points to `pnpm test` and fixture validation instead of silent manual claims. |
| T-01-04 | Information Disclosure | health docs | mitigate | Docs describe `/health` as generic local status, not external dependency health. |
| T-01-05 | Information Disclosure | dev integration docs | mitigate | Docs describe Next rewrite/same-origin local access rather than broad CORS. |
</threat_model>

<verification>
- `pnpm check`
- `pnpm test`
- `pnpm build`
- Manual: run `pnpm dev`, open the web app, and verify API Health is OK and Contract Fixture is Valid.
</verification>

<success_criteria>
- README documents setup, env vars, dev commands, and verification commands.
- App-specific `.env.example` files exist and keep secrets server-side.
- Workspace check, test, and build pass.
- Phase 1 is ready for execution verification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation-contracts/01-04-SUMMARY.md`.
</output>
