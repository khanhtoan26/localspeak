---
phase: 07-comprehensive-ui-ux-redesign-design-system
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - apps/web/components/json-analysis/ai-coach-tab.tsx
  - apps/web/components/json-analysis/json-input-card.tsx
  - apps/web/components/json-analysis/validation-preview-card.tsx
  - apps/web/components/json-analysis/saved-sessions-panel.tsx
  - apps/web/components/json-analysis/json-analysis-panel.tsx
  - apps/web/components/audio-mode/audio-mode-panel.tsx
autonomous: true
requirements:
  - UIX-04
  - UIX-05
  - UIX-06
  - UIX-07

must_haves:
  truths:
    - "json-analysis-panel.tsx renders PracticePriorityCard first, then SummaryMetricCards, then ResultTabs, then Collapsible JSON input"
    - "The JSON input Collapsible is auto-closed when analysis completes"
    - "Collapsible trigger button text is exactly 'Change JSON input' when closed"
    - "The 'Analyze Pronunciation' button text replaces 'Analyze JSON' (per UI-SPEC copywriting)"
    - "aria-label='Speech assessment JSON input' is preserved on the Textarea"
    - "json-analysis-panel.tsx uses Shadcn Collapsible from @/components/ui/collapsible"
    - "audio-mode-panel.tsx uses Shadcn Input and Card; no json-analysis-card class remains"
    - "label 'Reference sentence' with htmlFor='reference-text' is preserved in audio panel"
    - "All interactive elements in both panels have min-h-[44px]"
    - "No horizontal overflow: min-w-0 on all flex/grid children in both panels"
  artifacts:
    - path: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      provides: "Full JSON analysis panel with outcome-first hierarchy and Collapsible input"
    - path: "apps/web/components/json-analysis/ai-coach-tab.tsx"
      provides: "AI Coach tab with Shadcn Skeleton loading state and Button CTAs"
    - path: "apps/web/components/json-analysis/json-input-card.tsx"
      provides: "JSON input form with Shadcn Textarea and Button components"
    - path: "apps/web/components/json-analysis/validation-preview-card.tsx"
      provides: "Validation result card with Shadcn Card, Badge, and Button"
    - path: "apps/web/components/audio-mode/audio-mode-panel.tsx"
      provides: "Audio practice panel with Shadcn Input and Card"
  key_links:
    - from: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      to: "apps/web/components/ui/collapsible.tsx"
      via: "Shadcn Collapsible replaces native <details> element"
      pattern: "from.*@/components/ui/collapsible"
    - from: "apps/web/components/json-analysis/json-analysis-panel.tsx"
      to: "apps/web/components/json-analysis/summary-metric-cards.tsx"
      via: "SummaryMetricCards rendered inside main results section"
      pattern: "<SummaryMetricCards"
    - from: "apps/web/components/json-analysis/ai-coach-tab.tsx"
      to: "apps/web/components/ui/skeleton.tsx"
      via: "Shadcn Skeleton for AI feedback loading state"
      pattern: "from.*@/components/ui/skeleton"
    - from: "apps/web/components/audio-mode/audio-mode-panel.tsx"
      to: "apps/web/components/ui/input.tsx"
      via: "Shadcn Input for reference sentence text field"
      pattern: "from.*@/components/ui/input"
---

<objective>
Complete the JSON Analysis panel migration (ai-coach-tab, json-input-card, validation-preview-card, saved-sessions-panel, and the orchestrating json-analysis-panel) and migrate the Audio Mode panel. This plan delivers the outcome-first visual hierarchy (D-11, UIX-04) and the Collapsible JSON input pattern (D-11, UIX-02), and migrates the audio surface to its simple speaking-flow hierarchy (UIX-05).

Purpose: json-analysis-panel.tsx is the heaviest migration — 578 lines, orchestrates all sub-components, and adds new state (isInputOpen for Collapsible). It depends on all sub-components being migrated first (done in Plan 03). Audio panel is independent of the JSON tree and can be done in parallel, but is grouped here to keep context load manageable.

Output: All JSON analysis and audio components fully migrated to Tailwind/Shadcn. The app renders a complete redesigned UI end-to-end after this plan.
</objective>

<execution_context>
@/home/toanak/workspace/localspeak/.claude/get-shit-done/workflows/execute-plan.md
@/home/toanak/workspace/localspeak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-CONTEXT.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-PATTERNS.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-UI-SPEC.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-03-SUMMARY.md

<interfaces>
<!-- All className mappings are drawn from PATTERNS.md — use the exact strings specified there. -->
<!-- Executor MUST read each source file before modifying it. -->

From PATTERNS.md — json-analysis-panel.tsx changes summary:
  - Remove: all BEM class names (json-analysis-page, json-analysis-shell, json-analysis-header, etc.)
  - Add: isInputOpen useState for Collapsible
  - Add: auto-close Collapsible when analysisState transitions to "done": setIsInputOpen(false)
  - Replace: <details className="json-input-disclosure"> with Shadcn <Collapsible>
  - Collapsible trigger text: "Hide JSON input" when open, "Change JSON input" when closed
  - PracticePriorityCard must render FIRST in the results section (UIX-04 outcome-first)
  - New imports needed:
    import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible";
    import { Button } from "@/components/ui/button";
    import { Card } from "@/components/ui/card";
    import { Skeleton } from "@/components/ui/skeleton";
    import { ChevronDown } from "lucide-react";
    import { cn } from "@/lib/utils";

From PATTERNS.md — ai-coach-tab.tsx Skeleton replacement:
  // REPLACE the custom skeleton div with:
  <section className="flex flex-col gap-4 py-6" aria-busy="true">
    <div role="status" aria-label="Loading AI feedback" className="space-y-3">
      <Skeleton className="h-4 w-3/4" />
      <Skeleton className="h-4 w-1/2" />
      <Skeleton className="h-4 w-2/3" />
    </div>
    <p className="text-base text-muted-foreground">Generating personalized IELTS feedback…</p>
  </section>

From PATTERNS.md — json-input-card.tsx:
  Button text "Analyze JSON" → "Analyze Pronunciation" (UI-SPEC copywriting contract)
  aria-label="Speech assessment JSON input" on Textarea: MUST be preserved
  Textarea component: <Textarea className="font-mono min-h-[280px] resize-y bg-input w-full max-w-full" aria-label="Speech assessment JSON input" />

From PATTERNS.md — audio-mode-panel.tsx:
  Outer div: className="flex flex-col gap-4 rounded-[18px] border border-border bg-card p-4"
  Reference input: <Input className="bg-input" /> (Shadcn Input, not textarea)
  Label: htmlFor="reference-text", text "Reference sentence" — MUST be preserved exactly
  WordScoreCard: <Card className="p-4 min-w-0">
  Audio word chips: base="inline-flex items-center rounded-xl px-3 py-1.5 text-sm font-medium min-h-[44px]"
    good: "bg-[#f0f7f2] text-success border border-[#d9e8dd]"
    okay: "bg-[#fdf7ec] text-warning border border-[#eadcb8]"
    weak: "bg-[#fdf1ee] text-danger border border-[#edd0ca]"

Collapsible pattern (from PATTERNS.md Pattern 5):
  const [isInputOpen, setIsInputOpen] = useState(false);

  <Collapsible open={isInputOpen} onOpenChange={setIsInputOpen}>
    <CollapsibleTrigger asChild>
      <Button variant="ghost" className="w-full justify-between min-h-[44px] font-medium">
        {isInputOpen ? "Hide JSON input" : "Change JSON input"}
        <ChevronDown className={cn("h-4 w-4 transition-transform", isInputOpen && "rotate-180")} />
      </Button>
    </CollapsibleTrigger>
    <CollapsibleContent className="flex flex-col gap-3 pt-2">
      {resultsStale ? (
        <p className="text-sm text-warning font-medium">
          Input changed. Analyze again to update results.
        </p>
      ) : null}
      <JsonInputCard ... />
      <ValidationPreviewCard ... />
    </CollapsibleContent>
  </Collapsible>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ai-coach-tab.tsx, json-input-card.tsx, validation-preview-card.tsx, and saved-sessions-panel.tsx</name>
  <read_first>
    - apps/web/components/json-analysis/ai-coach-tab.tsx — must read 121-line file; note all state flow, button texts, and loading state structure
    - apps/web/components/json-analysis/json-input-card.tsx — must read 121-line file; confirm aria-label on textarea and "Analyze JSON" button text location
    - apps/web/components/json-analysis/validation-preview-card.tsx — must read 239-line file; note all state variants (loading, danger, warning, success) and badge variant usage
    - apps/web/components/json-analysis/saved-sessions-panel.tsx — must read; note Card, Button usage patterns
  </read_first>
  <files>
    apps/web/components/json-analysis/ai-coach-tab.tsx,
    apps/web/components/json-analysis/json-input-card.tsx,
    apps/web/components/json-analysis/validation-preview-card.tsx,
    apps/web/components/json-analysis/saved-sessions-panel.tsx
  </files>
  <action>
RULE: Replace only className strings and import statements. Do NOT change logic, prop types, state, fetch calls.

--- ai-coach-tab.tsx ---
Add imports:
```tsx
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
```

Replace className strings per PATTERNS.md:
- idle state section: `className="flex flex-col gap-4 py-6"`
- idle state detail p: `className="text-base text-muted-foreground"`
- idle state button: `<Button className="min-h-[44px]">Get AI Feedback</Button>`
- loading state section: `className="flex flex-col gap-4 py-6" aria-busy="true"`
- Replace the custom skeleton div entirely with:
  ```tsx
  <div role="status" aria-label="Loading AI feedback" className="space-y-3">
    <Skeleton className="h-4 w-3/4" />
    <Skeleton className="h-4 w-1/2" />
    <Skeleton className="h-4 w-2/3" />
  </div>
  ```
- loading p: `className="text-base text-muted-foreground"`
- error state: `<Card className="border-[#edd0ca] p-4 flex flex-col gap-3">`
- error title h2: `className="text-xl font-semibold text-foreground m-0"`
- error detail p: `className="text-base text-muted-foreground"`
- error retry button: `<Button className="min-h-[44px]">Retry AI Feedback</Button>`
- done state section: `className="flex flex-col gap-4"`
- done title h2: `className="text-xl font-semibold text-foreground m-0"`
- ai-coach-bands div: `className="grid grid-cols-2 gap-2"`
- ai-coach-band div: `<Card className="p-3 flex flex-col gap-1">`
- band label span: `className="font-mono text-[11px] font-semibold uppercase tracking-[0.06em] text-subtle"`
- band value span: `className="font-display text-3xl text-foreground"`
- ai-coach-section div: `className="flex flex-col gap-2"`
- section title h3: `className="text-base font-semibold text-foreground m-0"`
- ai-coach-errors ol: `className="flex flex-col gap-3 list-none p-0 m-0"`
- error li: `className="flex flex-col gap-1 text-sm rounded-xl border border-border p-3"`
- ai-coach-drills ol: `className="flex flex-col gap-2 list-decimal list-inside text-sm"`
- drill li: `className="text-sm"`

--- json-input-card.tsx ---
Add imports:
```tsx
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
```

Replace className strings per PATTERNS.md:
- outer section: `className="flex flex-col gap-4"`
- header div: `className="flex items-start justify-between gap-4"`
- title h2: `className="text-xl font-semibold text-foreground m-0"`
- mode pill span: `className="inline-flex items-center rounded-full bg-sidebar text-muted-foreground text-[11px] font-semibold uppercase tracking-[0.06em] px-2 py-1 shrink-0"`
- input label: `className="block font-mono text-[11px] font-semibold uppercase tracking-[0.06em] text-subtle mt-4"`
- Textarea: `<Textarea className="font-mono min-h-[280px] resize-y bg-input w-full max-w-full" aria-label="Speech assessment JSON input" />`
  CRITICAL: `aria-label="Speech assessment JSON input"` MUST be preserved on the Textarea
- actions div: `className="flex flex-wrap gap-2 mt-2"`
- file upload button (label element): `<Button variant="outline" size="sm" asChild>` wrapping the label
- file input: `className="sr-only"` (visually hidden inside button label)
- secondary buttons (Load sample, Clear): `<Button variant="outline" size="sm">`
- error p: `className="text-sm text-danger font-medium"`
- meta div: `className="flex flex-wrap items-center gap-2 mt-2 font-mono text-[11px] text-subtle"`
- analyze row div: `className="flex flex-wrap items-center gap-3 mt-4"`
- PRIMARY BUTTON — COPY CHANGE: Replace "Analyze JSON" with "Analyze Pronunciation":
  `<Button disabled={!canAnalyze} className="min-h-[44px]">Analyze Pronunciation</Button>`
- helper p: `className="text-sm text-muted-foreground"`

--- validation-preview-card.tsx ---
Add imports:
```tsx
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
```

Replace className strings per PATTERNS.md. The card variant per state:
- Default card: `<Card className="p-4 min-w-0">`
- Danger card: `<Card className="p-4 min-w-0 border-[#edd0ca]">`
- Warning card: `<Card className="p-4 min-w-0 border-[#eadcb8]">`
- Success card: `<Card className="p-4 min-w-0 border-[#d9e8dd]">`

Other replacements:
- `json-analysis-card__title` h2: `className="text-xl font-semibold text-foreground m-0"`
- `json-analysis-card__detail` p: `className="text-base text-muted-foreground mt-3"`
- `json-analysis-card__header` div: `className="flex items-start justify-between gap-4"`
- `json-analysis-pill--danger` span: `<Badge variant="destructive">`
- `json-analysis-pill--warning` span: `<Badge className="bg-warning text-white">`
- `json-issue-list` ul: `className="flex flex-col gap-2 mt-3 list-none p-0"`
- `json-issue-row` li: `className="flex flex-col gap-1 text-sm"`
- `json-analysis-card__meta` p: `className="font-mono text-[11px] text-subtle mt-2"`
- Show all issues button: `<Button variant="link" size="sm" className="p-0 h-auto">`
- `json-all-issues` div: `className="mt-3"`
- `json-analysis-subtitle` h3: `className="text-base font-semibold text-foreground m-0 mb-2"`
- `json-technical-details` div: `className="mt-3"`
- `json-technical-line` code: `className="block font-mono text-[11px] text-subtle bg-sidebar rounded p-2 mt-1 break-all"`

--- saved-sessions-panel.tsx ---
Read the current file first to identify className patterns. Apply the same Card/Button Shadcn substitution pattern used above:
- Replace any `json-analysis-card` wrappers with `<Card className="p-4 min-w-0">`
- Replace any `json-primary-button` / `json-secondary-button` with Shadcn Button
- Replace any BEM layout class names with equivalent Tailwind utilities
- Preserve all fetch logic, state, and prop handling exactly
  </action>
  <verify>
    <automated>
      grep -c '"Analyze Pronunciation"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-input-card.tsx
    </automated>
    <automated>
      grep -c '"Analyze JSON"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-input-card.tsx
    </automated>
    <automated>
      grep -c 'aria-label="Speech assessment JSON input"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-input-card.tsx
    </automated>
    <automated>
      grep -c 'from "@/components/ui/skeleton"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/ai-coach-tab.tsx
    </automated>
    <automated>
      grep -c 'role="status"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/ai-coach-tab.tsx
    </automated>
    <automated>
      grep -c 'from "@/components/ui/card"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/validation-preview-card.tsx
    </automated>
    <automated>
      pnpm --filter web test 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/json-analysis/json-input-card.tsx button text is "Analyze Pronunciation" (NOT "Analyze JSON")
    - apps/web/components/json-analysis/json-input-card.tsx Textarea has `aria-label="Speech assessment JSON input"`
    - apps/web/components/json-analysis/json-input-card.tsx imports `Textarea` from "@/components/ui/textarea"
    - apps/web/components/json-analysis/json-input-card.tsx imports `Button` from "@/components/ui/button"
    - apps/web/components/json-analysis/ai-coach-tab.tsx imports `Skeleton` from "@/components/ui/skeleton"
    - apps/web/components/json-analysis/ai-coach-tab.tsx contains `role="status"` on loading container
    - apps/web/components/json-analysis/ai-coach-tab.tsx does NOT contain `ai-coach-skeleton` className
    - apps/web/components/json-analysis/validation-preview-card.tsx imports `Card`, `Badge`, `Button` from @/components/ui/
    - apps/web/components/json-analysis/validation-preview-card.tsx does NOT contain `json-analysis-card` as a className string
    - `pnpm --filter web test` passes (json-analysis-panel.test.tsx, saved-sessions-panel.test.tsx must still pass)
  </acceptance_criteria>
  <done>AI Coach Skeleton, JSON input form, validation card, and saved sessions panel migrated to Shadcn components with Tailwind classes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate json-analysis-panel.tsx (outcome-first hierarchy + Collapsible) and audio-mode-panel.tsx</name>
  <read_first>
    - apps/web/components/json-analysis/json-analysis-panel.tsx — MUST read the full 578-line file before editing; note the <details> element location, analysisState transitions, PracticePriorityCard position, stale state pattern, and all existing useState declarations
    - apps/web/components/audio-mode/audio-mode-panel.tsx — must read 121-line file; note the reference input htmlFor/id pattern and all class names
    - .planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-03-SUMMARY.md — read to confirm which components were migrated and any TypeScript notes
  </read_first>
  <files>
    apps/web/components/json-analysis/json-analysis-panel.tsx,
    apps/web/components/audio-mode/audio-mode-panel.tsx
  </files>
  <action>
--- json-analysis-panel.tsx ---
This is the heaviest migration file. Follow this order:

1. Add new imports at the top:
```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
import { ChevronDown } from "lucide-react";
import { cn } from "@/lib/utils";
```

2. Add new state for Collapsible (alongside existing useState declarations):
```tsx
const [isInputOpen, setIsInputOpen] = useState(false);
```

3. Find the location where analysisState transitions to "done" (in the API call handler or wherever setAnalysisState("done") is called). After that transition, add:
```tsx
setIsInputOpen(false); // Auto-collapse input when analysis completes (UIX-02)
```

4. Replace ALL className strings per PATTERNS.md. Key structural replacements:
- `json-analysis-page` section → `className="flex flex-col gap-4"`
- `json-analysis-shell` section → `className="flex flex-col gap-4"`
- `json-analysis-header` header → `className="pt-2 pb-1"`
- `json-analysis-tag` span → `className="inline-flex items-center rounded-full bg-sidebar text-primary text-[11px] font-semibold uppercase tracking-[0.06em] px-2 py-1"`
- `json-analysis-title` h1 → `className="font-display text-3xl text-foreground mt-4 mb-2"`
- `json-analysis-intro` p → `className="text-base text-muted-foreground m-0"`
- `json-results-region` section → `className="flex flex-col gap-4"`
- `json-analysis-pill saved-session-marker` p → `className="inline-flex items-center rounded-full bg-sidebar text-muted-foreground text-[11px] font-semibold uppercase tracking-[0.06em] px-2 py-1"`
- Stale warning card: `<Card className="border-[#eadcb8] p-4 min-w-0">`
- Stale issue list ul: `className="flex flex-col gap-2 mt-3 list-none p-0"`
- `json-results-main` div → `className="flex flex-col gap-4"`
- Loading state p: replace with Skeleton similar to ai-coach-tab pattern
- Error card: `<Card className="border-[#edd0ca] p-4 min-w-0">`

5. Replace the `<details className="json-input-disclosure">` element with Shadcn Collapsible:
```tsx
<Collapsible open={isInputOpen} onOpenChange={setIsInputOpen}>
  <CollapsibleTrigger asChild>
    <Button variant="ghost" className="w-full justify-between min-h-[44px] font-medium">
      {isInputOpen ? "Hide JSON input" : "Change JSON input"}
      <ChevronDown className={cn("h-4 w-4 transition-transform", isInputOpen && "rotate-180")} />
    </Button>
  </CollapsibleTrigger>
  <CollapsibleContent className="flex flex-col gap-3 pt-2">
    {resultsStale ? (
      <p className="text-sm text-warning font-medium">
        Input changed. Analyze again to update results.
      </p>
    ) : null}
    <JsonInputCard {/* existing props unchanged */} />
    <ValidationPreviewCard {/* existing props unchanged */} />
  </CollapsibleContent>
</Collapsible>
```
Note: The stale warning banner moves inside the CollapsibleContent (it was previously outside the details element — check the current file to confirm exact location and move it as needed).

6. PracticePriorityCard className replacements (lines ~532-549):
- `json-priority-card` section → `<Card className="p-5 min-w-0 bg-sidebar border-border">`
- `json-analysis-pill` p → `className="inline-flex items-center rounded-full bg-background text-primary text-[11px] font-semibold uppercase tracking-[0.06em] px-2 py-1 mb-2"`
- `json-analysis-card__title` h2 → `className="text-xl font-semibold text-foreground m-0"`
  CRITICAL: heading text "What should I practice next?" must remain unchanged (Playwright test asserts on this)
- `json-priority-card__priority` p → `className="font-display text-2xl text-foreground mt-3 mb-1"`
- `json-priority-card__reason` p → `className="text-base text-muted-foreground m-0"`

7. Add min-w-0 to every flex/grid child container (UIX-06 overflow prevention):
- The main content wrapper div: `className="min-w-0"`
- Any div that is a direct child of a flex or grid container

--- audio-mode-panel.tsx ---
Add imports:
```tsx
import { Input } from "@/components/ui/input";
import { Card } from "@/components/ui/card";
import { cn } from "@/lib/utils";
```

Replace className strings per PATTERNS.md:
- Outer div: `className="flex flex-col gap-4 rounded-[18px] border border-border bg-card p-4"`
- Reference input label: `className="block font-mono text-[11px] font-semibold uppercase tracking-[0.06em] text-subtle"`
  CRITICAL: Keep `htmlFor="reference-text"` on label and `id="reference-text"` on Input. Label text must remain "Reference sentence".
- Reference input: Replace `<input className="json-input-textarea audio-reference-input" ...>` with:
  `<Input id="reference-text" className="bg-input" ... />`  (all other props unchanged)
- `audio-section` divs: `className="flex flex-col gap-2"`
- Error p: `className="text-sm text-danger font-medium"`
- WordScoreCard outer: `<Card className="p-4 min-w-0">`
- Score card header div: `className="flex items-center justify-between gap-4 mb-3"`
- Score label span: `className="font-mono text-[11px] font-semibold uppercase tracking-[0.06em] text-subtle"`
- Score value span (with level colors):
  ```tsx
  const scoreColor = {
    good: "text-success",
    okay: "text-warning",
    weak: "text-danger",
  };
  // className={`font-display text-3xl ${scoreColor[level]}`}
  ```
- Audio word chip list div: `className="flex flex-wrap gap-1.5"`
- Audio word chips: base `"inline-flex items-center rounded-xl px-3 py-1.5 text-sm font-medium min-h-[44px]"` + level color:
  ```tsx
  const chipColor = {
    good: "bg-[#f0f7f2] text-success border border-[#d9e8dd]",
    okay: "bg-[#fdf7ec] text-warning border border-[#eadcb8]",
    weak: "bg-[#fdf1ee] text-danger border border-[#edd0ca]",
  };
  ```
- Fluency meta div: `className="flex flex-wrap gap-3 font-mono text-[11px] text-subtle mt-2"`
- Score summary p: `className="text-sm text-muted-foreground mt-2"`
  </action>
  <verify>
    <automated>
      grep -c 'from "@/components/ui/collapsible"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-analysis-panel.tsx
    </automated>
    <automated>
      grep -c '"Change JSON input"' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-analysis-panel.tsx
    </automated>
    <automated>
      grep -c 'isInputOpen' /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-analysis-panel.tsx
    </automated>
    <automated>
      grep -c 'from "@/components/ui/input"' /home/toanak/workspace/localspeak/apps/web/components/audio-mode/audio-mode-panel.tsx
    </automated>
    <automated>
      grep -c 'Reference sentence' /home/toanak/workspace/localspeak/apps/web/components/audio-mode/audio-mode-panel.tsx
    </automated>
    <automated>
      grep -c 'json-analysis-card' /home/toanak/workspace/localspeak/apps/web/components/audio-mode/audio-mode-panel.tsx
    </automated>
    <automated>
      pnpm --filter web test 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/json-analysis/json-analysis-panel.tsx imports from "@/components/ui/collapsible"
    - apps/web/components/json-analysis/json-analysis-panel.tsx contains `isInputOpen` state declaration
    - apps/web/components/json-analysis/json-analysis-panel.tsx contains `"Change JSON input"` (Collapsible trigger text)
    - apps/web/components/json-analysis/json-analysis-panel.tsx contains `setIsInputOpen(false)` near the analysisState "done" transition
    - apps/web/components/json-analysis/json-analysis-panel.tsx does NOT contain `json-input-disclosure` className
    - apps/web/components/json-analysis/json-analysis-panel.tsx does NOT contain `<details` element (replaced by Collapsible)
    - apps/web/components/json-analysis/json-analysis-panel.tsx contains `"What should I practice next?"` heading text unchanged
    - apps/web/components/json-analysis/json-analysis-panel.tsx does NOT contain `json-analysis-card` as a className string
    - apps/web/components/audio-mode/audio-mode-panel.tsx imports `Input` from "@/components/ui/input"
    - apps/web/components/audio-mode/audio-mode-panel.tsx contains `"Reference sentence"` label text
    - apps/web/components/audio-mode/audio-mode-panel.tsx contains `htmlFor="reference-text"` on the label
    - apps/web/components/audio-mode/audio-mode-panel.tsx does NOT contain `json-analysis-card` as a className (the cross-component style reuse smell is eliminated)
    - apps/web/components/audio-mode/audio-mode-panel.tsx contains `min-h-[44px]` on word chips
    - `pnpm --filter web test` passes (json-analysis-panel.test.tsx must still pass)
  </acceptance_criteria>
  <done>json-analysis-panel.tsx fully migrated: outcome-first hierarchy, Shadcn Collapsible input, all BEM removed. audio-mode-panel.tsx migrated to Shadcn Input + Card with label preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User JSON input → analysis API | JSON paste in Textarea goes to /api/json-analysis/ — same-origin, unchanged from before migration |
| Audio microphone → Gemini Live | Audio flow unchanged; AudioModePanel migration is CSS-only (D-11, D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | <details> → Collapsible migration breaks JSON input disclosure state | mitigate | Collapsible `open={isInputOpen}` controlled state mirrors the previous `<details>` open attribute; auto-close `setIsInputOpen(false)` on analysis completion is explicit and tested via "Change JSON input" text presence assertion |
| T-07-04-02 | Tampering | "Analyze JSON" → "Analyze Pronunciation" copy change breaks Playwright test | mitigate | PATTERNS.md explicitly documents this Playwright selector change; Plan 05 (test update) will fix `getByRole("button", { name: "Analyze JSON" })` → `"Analyze Pronunciation"` |
| T-07-04-03 | Elevation of Privilege | Collapsible focus management — keyboard users can access JSON input from anywhere | accept | Shadcn Collapsible (Radix) handles focus management correctly; CollapsibleContent receives focus when opened via keyboard; no focus trapping since this is not a dialog |
| T-07-04-04 | Denial of Service | Audio reference input `htmlFor`/`id` mismatch breaks label-input association | mitigate | Acceptance criteria explicitly requires `htmlFor="reference-text"` on label and `id="reference-text"` on Input; `getByLabel("Reference sentence")` Playwright assertion in Plan 05 will catch regression |
| T-07-04-05 | Spoofing | json-analysis-card cross-component CSS bleed (audio panel uses json-analysis-card class) | mitigate | Acceptance criteria explicitly requires audio-mode-panel.tsx to NOT contain `json-analysis-card` className after migration; self-contained Tailwind classes replace the shared CSS dependency |
</threat_model>

<verification>
After both tasks complete:

1. Unit tests:
   ```bash
   pnpm --filter web test 2>&1 | tail -20
   ```
   Expected: all tests pass (0 failures). json-analysis-panel.test.tsx is the key file here.

2. Check for remaining BEM class names across all migrated JSON analysis files:
   ```bash
   grep -rn "json-analysis-card\|json-input-disclosure\|ai-coach-skeleton\|json-priority-card\b\|json-analyze-row\|audio-reference-input" \
     /home/toanak/workspace/localspeak/apps/web/components/
   ```
   Expected: no output

3. Confirm Collapsible state integration:
   ```bash
   grep -n "setIsInputOpen" /home/toanak/workspace/localspeak/apps/web/components/json-analysis/json-analysis-panel.tsx
   ```
   Expected: at least 2 lines — one useState declaration, one setIsInputOpen(false) call near the analysis completion transition

4. Dev server smoke test (app renders without errors):
   ```bash
   pnpm --filter web build 2>&1 | tail -20
   ```
   Expected: build succeeds with no TypeScript or compilation errors
</verification>

<success_criteria>
- json-analysis-panel.tsx: Shadcn Collapsible replaces native details element; isInputOpen state added; auto-closes on analysis completion; all BEM class names removed; PracticePriorityCard renders first in results hierarchy
- json-input-card.tsx: "Analyze Pronunciation" copy (not "Analyze JSON"); aria-label on Textarea preserved; Shadcn Textarea + Button
- ai-coach-tab.tsx: Shadcn Skeleton with role="status" replaces custom skeleton div; Button CTAs
- validation-preview-card.tsx: Shadcn Card variants for danger/warning/success states; Badge + Button
- audio-mode-panel.tsx: Shadcn Input with Reference sentence label (htmlFor preserved); Card for score; min-h-[44px] on word chips; no json-analysis-card cross-dependency
- `pnpm --filter web test` passes
- `pnpm --filter web build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-04-SUMMARY.md` using the template at `.claude/get-shit-done/templates/summary.md`.

Key items to record:
- Confirm "Analyze Pronunciation" copy is in place (critical for Plan 05 test update)
- Confirm "Change JSON input" text in Collapsible trigger (critical for Playwright assertion)
- Confirm "Reference sentence" label preserved in audio panel (critical for Playwright assertion)
- Confirm build passes with 0 TypeScript errors
- Note any edge cases in the details → Collapsible migration
</output>
</output>
