---
phase: 07-comprehensive-ui-ux-redesign-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/app/globals.css
  - apps/web/components.json
  - apps/web/lib/utils.ts
  - apps/web/components/ui/button.tsx
  - apps/web/components/ui/card.tsx
  - apps/web/components/ui/tabs.tsx
  - apps/web/components/ui/badge.tsx
  - apps/web/components/ui/input.tsx
  - apps/web/components/ui/textarea.tsx
  - apps/web/components/ui/separator.tsx
  - apps/web/components/ui/tooltip.tsx
  - apps/web/components/ui/dialog.tsx
  - apps/web/components/ui/skeleton.tsx
  - apps/web/components/ui/collapsible.tsx
  - apps/web/package.json
autonomous: true
requirements:
  - UIX-03

must_haves:
  truths:
    - "Running `pnpm --filter web dev` starts without CSS compilation errors"
    - "Tailwind utility classes apply in the browser (e.g., bg-background renders #fafaf7)"
    - "All 11 Shadcn component files exist in apps/web/components/ui/"
    - "apps/web/lib/utils.ts exports the cn() function"
    - "apps/web/components.json exists with correct aliases and style"
    - "TypeScript path alias @/* resolves in the editor without errors"
    - "Chromium Playwright binary is installed (pnpm exec playwright install chromium exits 0)"
  artifacts:
    - path: "apps/web/postcss.config.mjs"
      provides: "PostCSS plugin required for Tailwind v4 to compile"
    - path: "apps/web/app/globals.css"
      provides: "Tailwind v4 directives + @theme inline token block"
      contains: "@import \"tailwindcss\""
    - path: "apps/web/lib/utils.ts"
      provides: "cn() utility function"
      exports: ["cn"]
    - path: "apps/web/components.json"
      provides: "Shadcn CLI configuration"
    - path: "apps/web/components/ui/button.tsx"
      provides: "Shadcn Button component"
    - path: "apps/web/components/ui/card.tsx"
      provides: "Shadcn Card component"
    - path: "apps/web/components/ui/tabs.tsx"
      provides: "Shadcn Tabs component"
    - path: "apps/web/components/ui/collapsible.tsx"
      provides: "Shadcn Collapsible component"
  key_links:
    - from: "apps/web/postcss.config.mjs"
      to: "apps/web/app/globals.css"
      via: "@tailwindcss/postcss processes @import tailwindcss"
      pattern: "@tailwindcss/postcss"
    - from: "apps/web/tsconfig.json"
      to: "apps/web/components/ui/*.tsx"
      via: "@/* path alias allows shadcn imports"
      pattern: "\"@/*\": [\"./*\"]"
    - from: "apps/web/app/globals.css"
      to: "browser"
      via: "@theme inline token block provides CSS custom properties"
      pattern: "@theme inline"
---

<objective>
Install and configure the full Tailwind v4 + Shadcn/UI design system foundation before any component migration work begins. This plan creates the non-negotiable infrastructure that every subsequent plan depends on: the @/* TypeScript path alias, PostCSS config, globals.css replacement, Shadcn initialization, all 11 baseline components, the cn() utility, and the Playwright Chromium binary.

Purpose: Without this plan completed and verified, no Shadcn imports will resolve, no Tailwind classes will compile, and E2E tests cannot run. This is the critical path gating all other Phase 7 work.

Output:
- apps/web/tsconfig.json updated with @/* alias
- apps/web/postcss.config.mjs created
- apps/web/app/globals.css replaced with Tailwind v4 directives + warm color @theme block
- apps/web/components.json created by shadcn init
- apps/web/lib/utils.ts created with cn() function
- apps/web/components/ui/ populated with 11 Shadcn components
- lucide-react installed
- Playwright Chromium binary installed
</objective>

<execution_context>
@/home/toanak/workspace/localspeak/.claude/get-shit-done/workflows/execute-plan.md
@/home/toanak/workspace/localspeak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-CONTEXT.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-RESEARCH.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-PATTERNS.md
@.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-UI-SPEC.md

<interfaces>
<!-- Current tsconfig.json (apps/web/tsconfig.json) — no paths or baseUrl. Adding @/* is the FIRST step. -->
<!-- Current globals.css is 1,332 lines of BEM-ish CSS — FULL REPLACEMENT required (D-13) -->

Target globals.css structure (PATTERNS.md):
```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: #fafaf7;
  --color-card: #ffffff;
  --color-foreground: #161513;
  --color-muted-foreground: #5a564f;
  --color-subtle: #9b968d;
  --color-border: #ebe7df;
  --color-sidebar: #f1ede4;
  --color-input: #fffdf9;
  --color-primary: #d97757;
  --color-primary-foreground: #ffffff;
  --color-success: #3f6b4f;
  --color-warning: #b88a3e;
  --color-danger: #9f2d20;
  --color-destructive: #9f2d20;
  --color-success-border: #d9e8dd;
  --color-warning-border: #eadcb8;
  --color-danger-border: #edd0ca;
  --font-sans: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
  --font-display: "Instrument Serif", "Iowan Old Style", Georgia, serif;
  --radius: 1.125rem;
}

* { box-sizing: border-box; }
html, body { min-height: 100%; }
body { margin: 0; }
button, input, textarea, select { font: inherit; }
```

Target tsconfig.json additions:
```json
"baseUrl": ".",
"paths": { "@/*": ["./*"] }
```

Target postcss.config.mjs:
```js
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

lib/utils.ts (created by shadcn init — verify content):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @/* path alias and install Tailwind v4 + create postcss.config.mjs</name>
  <read_first>
    - apps/web/tsconfig.json — must read before editing to see current compilerOptions structure
    - apps/web/package.json — must read before installing to see current devDependencies
  </read_first>
  <files>
    apps/web/tsconfig.json,
    apps/web/postcss.config.mjs,
    apps/web/package.json
  </files>
  <action>
Step 1 — Edit apps/web/tsconfig.json. Add `"baseUrl": "."` and `"paths": { "@/*": ["./*"] }` to the existing `compilerOptions` object. Preserve all existing compilerOptions, include/exclude arrays. Do NOT remove any existing keys.

Step 2 — Create apps/web/postcss.config.mjs with exactly:
```js
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

Step 3 — From the monorepo root, run the package install. All three packages must be installed as devDependencies in apps/web:
```bash
pnpm --filter web add -D tailwindcss @tailwindcss/postcss tw-animate-css
```

Then install lucide-react as a runtime dependency:
```bash
pnpm --filter web add lucide-react
```

CRITICAL: Do NOT create a tailwind.config.ts or tailwind.config.js — Tailwind v4 uses CSS-first config via @theme inline in globals.css. No JS config file is needed or correct.
  </action>
  <verify>
    <automated>
      grep -c '"@/\*"' /home/toanak/workspace/localspeak/apps/web/tsconfig.json
    </automated>
    <automated>
      grep -c '"@tailwindcss/postcss"' /home/toanak/workspace/localspeak/apps/web/postcss.config.mjs
    </automated>
    <automated>
      grep '"tailwindcss"' /home/toanak/workspace/localspeak/apps/web/package.json
    </automated>
    <automated>
      grep '"lucide-react"' /home/toanak/workspace/localspeak/apps/web/package.json
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/tsconfig.json contains `"@/*": ["./*"]` in compilerOptions.paths
    - apps/web/tsconfig.json contains `"baseUrl": "."` in compilerOptions
    - apps/web/postcss.config.mjs exists and contains `"@tailwindcss/postcss": {}`
    - apps/web/package.json devDependencies contains "tailwindcss" and "@tailwindcss/postcss" and "tw-animate-css"
    - apps/web/package.json dependencies contains "lucide-react"
    - apps/web/tailwind.config.ts does NOT exist (Tailwind v4 uses CSS-first config)
  </acceptance_criteria>
  <done>Path alias ready, Tailwind v4 packages installed, PostCSS config in place.</done>
</task>

<task type="auto">
  <name>Task 2: Replace globals.css with Tailwind v4 directives + warm @theme token block, then run shadcn init and install all 11 components</name>
  <read_first>
    - apps/web/app/globals.css — must read the current file (1,332 lines) before replacing; verify no component styles are being carried forward (D-13 requires full replacement)
  </read_first>
  <files>
    apps/web/app/globals.css,
    apps/web/components.json,
    apps/web/lib/utils.ts,
    apps/web/components/ui/button.tsx,
    apps/web/components/ui/card.tsx,
    apps/web/components/ui/tabs.tsx,
    apps/web/components/ui/badge.tsx,
    apps/web/components/ui/input.tsx,
    apps/web/components/ui/textarea.tsx,
    apps/web/components/ui/separator.tsx,
    apps/web/components/ui/tooltip.tsx,
    apps/web/components/ui/dialog.tsx,
    apps/web/components/ui/skeleton.tsx,
    apps/web/components/ui/collapsible.tsx
  </files>
  <action>
Step 1 — Replace apps/web/app/globals.css entirely with the Tailwind v4 CSS-first config. Write ONLY the content below — do not preserve any existing BEM-ish class names or component styles (D-13: globals.css fully replaced):

```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  /* Color palette — maps from existing :root tokens */
  --color-background: #fafaf7;
  --color-card: #ffffff;
  --color-foreground: #161513;
  --color-muted-foreground: #5a564f;
  --color-subtle: #9b968d;
  --color-border: #ebe7df;
  --color-sidebar: #f1ede4;
  --color-input: #fffdf9;
  --color-primary: #d97757;
  --color-primary-foreground: #ffffff;
  --color-success: #3f6b4f;
  --color-warning: #b88a3e;
  --color-danger: #9f2d20;
  --color-destructive: #9f2d20;

  /* Semantic surface tokens for status card borders */
  --color-success-border: #d9e8dd;
  --color-warning-border: #eadcb8;
  --color-danger-border: #edd0ca;

  /* Typography */
  --font-sans: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
  --font-display: "Instrument Serif", "Iowan Old Style", Georgia, serif;

  /* Radius */
  --radius: 1.125rem;
}

/* Global resets only — no component-level classes */
* { box-sizing: border-box; }
html, body { min-height: 100%; }
body { margin: 0; }
button, input, textarea, select { font: inherit; }
```

Step 2 — Run shadcn init from inside apps/web/. This command is interactive — when prompted:
- Style: New York
- Base color: Neutral
- CSS variables: Yes
After init, shadcn may rewrite globals.css with its own tokens. If it does, restore the @theme inline block content above — the shadcn defaults use HSL values but the project uses hex. The @theme block above must be preserved exactly.

Run from the monorepo root:
```bash
cd apps/web && pnpm dlx shadcn@latest init
```

Step 3 — Install all 11 required Shadcn components (D-04 per CONTEXT.md):
```bash
cd apps/web && pnpm dlx shadcn@latest add button card tabs badge input textarea separator tooltip dialog skeleton collapsible
```

Step 4 — Install Playwright Chromium binary (required before any E2E runs; per VALIDATION.md Wave 0 requirement):
```bash
cd apps/web && pnpm exec playwright install chromium
```

After all steps: verify globals.css starts with `@import "tailwindcss"` (not `@tailwind base` which is v3 syntax). If shadcn init overwrote the file with v3-style directives, rewrite it with the Tailwind v4 content above.
  </action>
  <verify>
    <automated>
      head -3 /home/toanak/workspace/localspeak/apps/web/app/globals.css
    </automated>
    <automated>
      grep -c '@theme inline' /home/toanak/workspace/localspeak/apps/web/app/globals.css
    </automated>
    <automated>
      ls /home/toanak/workspace/localspeak/apps/web/components/ui/ | sort
    </automated>
    <automated>
      grep -c 'export function cn' /home/toanak/workspace/localspeak/apps/web/lib/utils.ts
    </automated>
    <automated>
      grep -c '"style": "new-york"' /home/toanak/workspace/localspeak/apps/web/components.json
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/globals.css line 1 is `@import "tailwindcss";` (NOT `@tailwind base`)
    - apps/web/app/globals.css contains `@theme inline`
    - apps/web/app/globals.css contains `--color-primary: #d97757`
    - apps/web/app/globals.css contains `--font-display:` declaration
    - apps/web/app/globals.css does NOT contain `.json-analysis` (no BEM class names remain)
    - apps/web/app/globals.css does NOT contain `@tailwind base` (Tailwind v3 syntax forbidden)
    - apps/web/lib/utils.ts exists and contains `export function cn`
    - apps/web/components.json exists
    - apps/web/components/ui/ directory contains: button.tsx, card.tsx, tabs.tsx, badge.tsx, input.tsx, textarea.tsx, separator.tsx, tooltip.tsx, dialog.tsx, skeleton.tsx, collapsible.tsx (11 files)
    - `pnpm exec playwright install chromium` exits 0 (Chromium binary installed)
  </acceptance_criteria>
  <done>Tailwind v4 directives active, warm @theme tokens declared, all 11 Shadcn components installed, cn() utility available, Playwright Chromium binary installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI tools → filesystem | shadcn CLI writes to apps/web/ — scope is controlled by running from apps/web/ directory |
| npm registry → installed packages | packages come from npm registry; all are official packages from established maintainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | shadcn init rewriting globals.css | mitigate | After shadcn init, verify globals.css line 1 is `@import "tailwindcss"` not v3 directives; restore @theme block if overwritten |
| T-07-01-02 | Tampering | shadcn add writing to wrong directory | mitigate | Run shadcn CLI from inside apps/web/ not monorepo root; verify components.json aliases.components points to @/components/ui |
| T-07-01-03 | Denial of Service | Missing PostCSS config silently breaks all Tailwind classes | mitigate | Verify postcss.config.mjs exists before any dev server or build run; acceptance criteria includes file content check |
| T-07-01-04 | Information Disclosure | Third-party Shadcn registry components | accept | All 11 components are from the official shadcn registry (ui.shadcn.com); no third-party registry blocks declared (per UI-SPEC registry safety section) |
</threat_model>

<verification>
After both tasks complete, run from monorepo root:

1. TypeScript compilation check:
   ```bash
   pnpm --filter web exec tsc --noEmit 2>&1 | head -20
   ```
   Expected: 0 errors related to @/* imports (existing errors from unmigrated components are acceptable — they will be fixed in later plans)

2. Dev server smoke check:
   ```bash
   pnpm --filter web dev &
   sleep 8
   curl -s -o /dev/null -w "%{http_code}" http://localhost:3000
   kill %1
   ```
   Expected: 200 (page loads, even if unstyled during transition)

3. Shadcn component count:
   ```bash
   ls /home/toanak/workspace/localspeak/apps/web/components/ui/ | wc -l
   ```
   Expected: 11 (button, card, tabs, badge, input, textarea, separator, tooltip, dialog, skeleton, collapsible)

4. Globals.css line count sanity:
   ```bash
   wc -l /home/toanak/workspace/localspeak/apps/web/app/globals.css
   ```
   Expected: fewer than 35 lines (the full replacement is ~32 lines; old 1,332-line file is gone)
</verification>

<success_criteria>
- apps/web/tsconfig.json has baseUrl "." and paths "@/*": ["./*"]
- apps/web/postcss.config.mjs exists with @tailwindcss/postcss plugin
- apps/web/app/globals.css is fully replaced: starts with @import "tailwindcss", contains @theme inline with all color/font/radius tokens, has NO BEM class names
- apps/web/lib/utils.ts exports cn() function
- apps/web/components.json exists and is valid JSON
- All 11 Shadcn components exist in apps/web/components/ui/
- lucide-react is in package.json dependencies
- Playwright Chromium binary is installed (pnpm exec playwright install chromium exits 0)
- No tailwind.config.ts or tailwind.config.js exists (Tailwind v4 does not use JS config)
</success_criteria>

<output>
After completion, create `.planning/phases/07-comprehensive-ui-ux-redesign-design-system/07-01-SUMMARY.md` using the template at `.claude/get-shit-done/templates/summary.md`.

Key items to record in summary:
- Exact shadcn init answers chosen (style, color, CSS variables)
- Whether shadcn init overwrote globals.css and needed restoration
- Final globals.css line count
- Any shadcn component install failures/retries
- Confirmation that Chromium binary installed successfully
</output>
