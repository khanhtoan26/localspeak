---
phase: "07.1-ground-up-shadcn-ui-rebuild"
plan: "05"
type: execute
wave: 5
depends_on:
  - "07.1-03"
  - "07.1-04"
files_modified:
  - "apps/web/app/page.test.tsx"
  - "apps/web/components/json-analysis/json-analysis-panel.test.tsx"
  - "apps/web/components/audio-mode/audio-mode-panel.test.tsx"
  - "apps/web/e2e/dashboard-ui.spec.ts"
  - "apps/web/e2e/responsive.spec.ts"
autonomous: true
requirements:
  - UIX-01
  - UIX-02
  - UIX-03
  - UIX-04
  - UIX-05
  - UIX-06
  - UIX-07
  - UIX-08
must_haves:
  truths:
    - "Unit tests preserve no duplicate full content panels, JSON behavior, and Live Audio behavior."
    - "E2E tests verify shadcn Sidebar shell, enabled JSON/Audio nav, disabled IELTS/TOEIC coming-soon entries, and no old mobile bottom nav."
    - "Responsive tests preserve 390px no-horizontal-overflow, labels, touch targets, keyboard/focus, contrast, JSON behavior, and audio behavior."
    - "Full phase gate commands pass."
  artifacts:
    - path: "apps/web/app/page.test.tsx"
      provides: "Shell and duplicate content panel guard"
    - path: "apps/web/components/json-analysis/json-analysis-panel.test.tsx"
      provides: "JSON behavior regression coverage"
    - path: "apps/web/components/audio-mode/audio-mode-panel.test.tsx"
      provides: "Live Audio behavior regression coverage"
    - path: "apps/web/e2e/dashboard-ui.spec.ts"
      provides: "New shadcn shell, JSON flow, audio flow E2E coverage"
    - path: "apps/web/e2e/responsive.spec.ts"
      provides: "390px no-overflow and accessibility coverage"
  key_links:
    - from: "dashboard-ui.spec.ts"
      to: "app shell"
      via: "Playwright role and text assertions"
      pattern: "Sidebar|JSON Analysis|Live Audio Practice|Coming soon"
    - from: "responsive.spec.ts"
      to: "390px layout"
      via: "scrollWidth <= clientWidth"
      pattern: "scrollWidth"
    - from: "json-analysis-panel.test.tsx"
      to: "Zod/API behavior"
      via: "existing mocked fetch flows"
      pattern: "preview|Analyze Pronunciation|Saved sessions"
---

<objective>
Update unit and E2E coverage for the Phase 07.1 shadcn rebuild.

Purpose: Satisfies UIX-08 and validates UIX-01 through UIX-07. Tests must protect the new app shell, JSON Analysis behavior, Live Audio behavior, responsive layout, accessibility basics, and anti-reuse constraints.

Output: Updated Vitest and Playwright tests plus full phase gate validation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-CONTEXT.md
@.planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-RESEARCH.md
@.planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-UI-SPEC.md
@.planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-VALIDATION.md
@.planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-PATTERNS.md
@apps/web/app/page.test.tsx
@apps/web/components/json-analysis/json-analysis-panel.test.tsx
@apps/web/e2e/dashboard-ui.spec.ts
@apps/web/e2e/responsive.spec.ts
@apps/web/e2e/fixtures/analysis.ts
@apps/web/components/app-shell/local-speak-workspace.tsx
@apps/web/components/app-shell/app-sidebar.tsx
@apps/web/components/json-analysis/json-analysis-panel.tsx
@apps/web/components/audio-mode/audio-mode-panel.tsx

<interfaces>
Allowed verification commands:
- `pnpm --filter web check`
- `pnpm --filter web test`
- `pnpm --filter web test:e2e -- dashboard-ui.spec.ts responsive.spec.ts`
- `pnpm --filter web test:e2e`

Required test coverage:
- UIX-01: shadcn Sidebar shell, JSON/Audio nav, disabled IELTS/TOEIC coming-soon, mobile drawer, no fixed bottom nav.
- UIX-02: primary next action visible in JSON empty/result and Audio no-reference/ready/recording states.
- UIX-03: tokenized design-system components and contrast checks.
- UIX-04: JSON input/validation/result order, stale behavior, saved sessions, AI feedback.
- UIX-05: Audio reference/readiness/recording/transcript/score states.
- UIX-06: 390px no horizontal overflow.
- UIX-07: keyboard navigation, focus, landmarks, labels, touch targets, contrast, screen-reader status copy.
- UIX-08: all above protected by unit + E2E regression suite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update unit/component tests for shell, JSON, and audio behavior</name>
  <files>
apps/web/app/page.test.tsx,
apps/web/components/json-analysis/json-analysis-panel.test.tsx,
apps/web/components/audio-mode/audio-mode-panel.test.tsx
  </files>
  <read_first>
- apps/web/app/page.test.tsx
- apps/web/components/json-analysis/json-analysis-panel.test.tsx
- apps/web/components/app-shell/local-speak-workspace.tsx
- apps/web/components/app-shell/surface-renderer.tsx
- apps/web/components/json-analysis/json-analysis-panel.tsx
- apps/web/components/audio-mode/audio-mode-panel.tsx
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-CONTEXT.md D-01 through D-17
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-UI-SPEC.md "Testing and Verification Contract" and "Regression Guards"
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-VALIDATION.md "Wave 0 Requirements" and requirement map
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-PATTERNS.md "Unit/component tests"
  </read_first>
  <action>
Update/create unit tests.

`apps/web/app/page.test.tsx`:
- Replace old duplicate desktop/mobile nav assumptions.
- Assert default JSON surface is active and `Speech assessment JSON input` appears exactly once.
- Assert clicking `Live Audio Practice` switches active surface and reveals `Reference sentence`.
- Assert disabled future entries `IELTS Practice` and `TOEIC Practice` render with `Coming soon` and do not switch to a real surface.
- Assert no text `Gemini Live`.
- Assert no text `Premium coach`, `Premium pronunciation coach`, or `Know exactly what to practice next.`.

`apps/web/components/json-analysis/json-analysis-panel.test.tsx`:
- Preserve existing tests for preview debounce, accepted-for-analysis gate, warning display, stale result behavior, AI feedback request/retry, and saved-session save/list/reopen.
- Update selectors/copy to required strings:
  - `Add speech assessment JSON`
  - `Analyze Pronunciation`
  - `What should I practice next?`
  - `Review the evidence`
  - `Saved sessions`
  - `Change JSON input`
  - `Input changed. Analyze again to update these results.`
- Preserve assertions that Zod-parsed API behavior is exercised through mocked responses.

Create `apps/web/components/audio-mode/audio-mode-panel.test.tsx` if it does not exist:
- Mock `useDeepgramSession`.
- Assert record control button `Start Recording` is disabled when `Reference sentence` is empty.
- Assert entering reference text enables `Start Recording`.
- Assert connecting state shows `Connecting to live transcription...`.
- Assert recording state shows `Stop Recording` and `Recording now. Speak clearly and finish the full sentence.`
- Assert empty transcript copy `Your live transcript will appear here.`
- Assert complete state renders `Pronunciation result` when mocked transcript words produce scoring.

Do not add new tooling. Use existing Vitest + Testing Library patterns.
  </action>
  <verify>
    <automated>pnpm --filter web test</automated>
  </verify>
  <acceptance_criteria>
- `grep -R "Speech assessment JSON input" apps/web/app/page.test.tsx` returns a match.
- `grep -R "toHaveLength(1)" apps/web/app/page.test.tsx` returns a match for the duplicate content panel guard.
- `grep -R "IELTS Practice\\|TOEIC Practice\\|Coming soon" apps/web/app/page.test.tsx` returns matches.
- `grep -R "Premium coach\\|Premium pronunciation coach\\|Know exactly what to practice next" apps/web/app/page.test.tsx` returns matches only in negative assertions using `queryByText`.
- `grep -R "Input changed. Analyze again to update these results." apps/web/components/json-analysis/json-analysis-panel.test.tsx` returns a match.
- `grep -R "Saved sessions" apps/web/components/json-analysis/json-analysis-panel.test.tsx` returns a match.
- `test -f apps/web/components/audio-mode/audio-mode-panel.test.tsx` succeeds.
- `grep -R "Start Recording\\|Stop Recording\\|Connecting to live transcription\\|Pronunciation result" apps/web/components/audio-mode/audio-mode-panel.test.tsx` returns matches.
- `pnpm --filter web test` exits 0.
  </acceptance_criteria>
  <done>
- All acceptance criteria above pass.
- The automated verification command in <verify> exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard E2E coverage for shadcn shell, JSON flow, and audio flow</name>
  <files>apps/web/e2e/dashboard-ui.spec.ts</files>
  <read_first>
- apps/web/e2e/dashboard-ui.spec.ts
- apps/web/e2e/fixtures/analysis.ts
- apps/web/components/app-shell/app-sidebar.tsx
- apps/web/components/app-shell/app-header.tsx
- apps/web/components/json-analysis/json-analysis-panel.tsx
- apps/web/components/audio-mode/audio-mode-panel.tsx
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-CONTEXT.md D-01 through D-17
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-UI-SPEC.md "Testing and Verification Contract"
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-VALIDATION.md requirement map
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-PATTERNS.md "Playwright tests"
  </read_first>
  <action>
Rewrite `apps/web/e2e/dashboard-ui.spec.ts` to target the new shadcn shell and preserve behavior.

Required tests:
1. `shadcn app shell exposes enabled practice surfaces and disabled future surfaces`
   - `page.goto("/")`
   - Assert `LocalSpeak` visible.
   - Assert subtitle `IELTS speaking practice` visible.
   - Assert buttons/menu items `JSON Analysis` and `Live Audio Practice` visible.
   - Assert `IELTS Practice`, `TOEIC Practice`, and `Coming soon` visible.
   - Assert no `Premium coach`, no `Premium pronunciation coach`, no fixed bottom nav text `Live Audio` as old mobile label.

2. `JSON Analysis preserves input validation and outcome-first result order`
   - Use `mockDashboardApi(page)`.
   - Fill `Speech assessment JSON input` with valid mocked JSON.
   - Assert `Analyze Pronunciation` becomes enabled.
   - Click analyze.
   - Assert visible order can be checked by locator positions:
     - `What should I practice next?`
     - metric labels via `data-testid="summary-metric-label"`
     - `Review the evidence`
     - `Saved sessions`
     - `Change JSON input`
   - Assert `Input changed. Analyze again to update these results.` appears after editing input post-result.
   - Assert `IELTS Analysis` tab and `Get AI Feedback` remain reachable.

3. `Live Audio Practice preserves no-reference and ready states`
   - Click `Live Audio Practice`.
   - Assert `Reference sentence`.
   - Assert `Enter one sentence first. Recording stays disabled until LocalSpeak knows what you want to practice.`
   - Assert `Start Recording` disabled.
   - Fill reference with `The trees stood near the street.`
   - Assert `Start Recording` enabled.
   - Assert `Your live transcript will appear here.` visible.

Use role/label selectors, not implementation-only class selectors. Do not add new Playwright tooling.
  </action>
  <verify>
    <automated>pnpm --filter web test:e2e -- dashboard-ui.spec.ts responsive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `grep -R "shadcn app shell" apps/web/e2e/dashboard-ui.spec.ts` returns a match.
- `grep -R "IELTS Practice\\|TOEIC Practice\\|Coming soon" apps/web/e2e/dashboard-ui.spec.ts` returns matches.
- `grep -R "Premium coach\\|Premium pronunciation coach" apps/web/e2e/dashboard-ui.spec.ts` returns matches only in negative assertions.
- `grep -R "What should I practice next?\\|Review the evidence\\|Saved sessions\\|Change JSON input" apps/web/e2e/dashboard-ui.spec.ts` returns matches.
- `grep -R "Input changed. Analyze again to update these results." apps/web/e2e/dashboard-ui.spec.ts` returns a match.
- `grep -R "Reference sentence\\|Start Recording\\|Your live transcript will appear here." apps/web/e2e/dashboard-ui.spec.ts` returns matches.
- `pnpm --filter web test:e2e -- dashboard-ui.spec.ts responsive.spec.ts` exits 0.
  </acceptance_criteria>
  <done>
- All acceptance criteria above pass.
- The automated verification command in <verify> exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update responsive, accessibility, contrast, and full phase gates</name>
  <files>apps/web/e2e/responsive.spec.ts</files>
  <read_first>
- apps/web/e2e/responsive.spec.ts
- apps/web/e2e/dashboard-ui.spec.ts
- apps/web/e2e/fixtures/analysis.ts
- apps/web/app/globals.css
- apps/web/components/app-shell/app-sidebar.tsx
- apps/web/components/app-shell/app-header.tsx
- apps/web/components/json-analysis/result-tabs.tsx
- apps/web/components/audio-mode/audio-mode-panel.tsx
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-UI-SPEC.md "Responsive Contract", "Accessibility Contract", "Testing and Verification Contract", "Explicit Do-Not-Reuse List"
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-VALIDATION.md "Requirement Verification Map"
- .planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-PATTERNS.md "Playwright tests"
  </read_first>
  <action>
Rewrite `apps/web/e2e/responsive.spec.ts` for the shadcn shell.

Required coverage:
1. Desktop shell:
   - At 1280px, assert shadcn/sidebar navigation is visible through `JSON Analysis`, `Live Audio Practice`, `IELTS Practice`, `TOEIC Practice`.
   - Assert there is no fixed bottom nav by checking no visible `nav` with old mobile-only fixed bottom behavior and no button named exact old label `Live Audio`.

2. Mobile shell:
   - At 390x844, assert no horizontal overflow on initial JSON state.
   - Assert sidebar trigger/menu button is visible and keyboard focusable.
   - Open mobile drawer with the trigger and assert `JSON Analysis`, `Live Audio Practice`, `IELTS Practice`, `TOEIC Practice`, and `Coming soon`.
   - Assert no fixed bottom nav.

3. JSON no-overflow:
   - At 390px, run JSON pre-analysis and post-analysis flow.
   - Assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth`.
   - Assert tabs remain role-based:
     - role `tablist`
     - tab `Pause Analysis`
     - tab `Words`
     - tab `Phonemes`
   - Assert ArrowRight changes selected tab.

4. Audio no-overflow:
   - At 390px, switch to Live Audio Practice.
   - Assert `Reference sentence`, disabled `Start Recording`, readiness copy, and no horizontal overflow.
   - Fill reference, assert `Start Recording` visible/enabled and at least 44px tall.

5. Accessibility and contrast:
   - Assert labels:
     - `Speech assessment JSON input`
     - `Upload .json file`
     - `Analyze Pronunciation`
     - `Reference sentence`
     - `Start Recording`
   - Assert body foreground/background contrast >= 4.5 using existing contrast helper.
   - Update expected body background RGB to `[250, 250, 250]` for `#FAFAFA`.
   - Update accent expectation to `[37, 99, 235]` for `#2563EB` if checking active/focus/accent color.
   - Assert focus reaches sidebar trigger and enabled nav controls by keyboard.

6. Anti-reuse guards:
   - Assert page does not show:
     - `Premium coach`
     - `Premium pronunciation coach`
     - `Know exactly what to practice next.`
     - old bottom nav exact label `Live Audio` when `Live Audio Practice` is the new label.

After updating the file, run all allowed phase gates in order.
  </action>
  <verify>
    <automated>pnpm --filter web check && pnpm --filter web test && pnpm --filter web test:e2e</automated>
  </verify>
  <acceptance_criteria>
- `grep -R "390" apps/web/e2e/responsive.spec.ts` returns matches.
- `grep -R "scrollWidth" apps/web/e2e/responsive.spec.ts` returns matches.
- `grep -R "Sidebar\\|sidebar trigger\\|JSON Analysis\\|Live Audio Practice\\|IELTS Practice\\|TOEIC Practice" apps/web/e2e/responsive.spec.ts` returns matches.
- `grep -R "Pause Analysis\\|Words\\|Phonemes\\|ArrowRight" apps/web/e2e/responsive.spec.ts` returns matches.
- `grep -R "Speech assessment JSON input\\|Upload .json file\\|Analyze Pronunciation\\|Reference sentence\\|Start Recording" apps/web/e2e/responsive.spec.ts` returns matches.
- `grep -R "250, 250, 250\\|37, 99, 235" apps/web/e2e/responsive.spec.ts` returns matches for updated token expectations if RGB checks are present.
- `grep -R "Premium coach\\|Premium pronunciation coach\\|Know exactly what to practice next" apps/web/e2e/responsive.spec.ts` returns matches only in negative assertions.
- `pnpm --filter web check` exits 0.
- `pnpm --filter web test` exits 0.
- `pnpm --filter web test:e2e -- dashboard-ui.spec.ts responsive.spec.ts` exits 0.
- `pnpm --filter web test:e2e` exits 0.
  </acceptance_criteria>
  <done>
- All acceptance criteria above pass.
- The automated verification command in <verify> exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| mocked API -> UI tests | Test fixtures simulate backend responses; tests must continue validating Zod/API behavior through UI flows. |
| browser viewport -> responsive shell | Responsive behavior can hide controls or create overflow. |
| keyboard/screen reader -> UI | Accessibility contracts rely on roles, labels, focus states, and semantic landmarks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07.1-05-01 | Tampering | tests masking JSON behavior regressions | mitigate | Preserve tests for preview debounce, accepted-for-analysis gate, warnings, stale state, AI feedback, saved-session save/list/reopen. |
| T-07.1-05-02 | Information Disclosure | frontend provider boundary | mitigate | Add/retain test guards that no `Gemini Live` or frontend provider-key behavior is introduced in this UI phase. |
| T-07.1-05-03 | Denial of Service | mobile overflow | mitigate | Playwright checks `scrollWidth <= clientWidth` at 390px for shell, JSON, audio, tabs, chips, and recording control. |
| T-07.1-05-04 | Spoofing | disabled future nav | mitigate | E2E asserts IELTS/TOEIC entries are disabled/coming soon and do not present real feature flows. |
| T-07.1-05-05 | Elevation of Privilege | auth/schema/provider scope creep | mitigate | Full test update touches frontend tests only; no backend/auth/schema/provider files are in scope. |
</threat_model>

<verification>
Run after Task 1:
- `pnpm --filter web test`

Run after Task 2:
- `pnpm --filter web test:e2e -- dashboard-ui.spec.ts responsive.spec.ts`

Run after Task 3 and before summary:
- `pnpm --filter web check`
- `pnpm --filter web test`
- `pnpm --filter web test:e2e -- dashboard-ui.spec.ts responsive.spec.ts`
- `pnpm --filter web test:e2e`
</verification>

<success_criteria>
- Unit tests pass and preserve no duplicate content panel, JSON behavior, and audio behavior.
- Targeted E2E tests pass for dashboard and responsive specs.
- Full E2E suite passes.
- Tests cover UIX-01 through UIX-08.
- Tests assert the anti-reuse list is gone or only present in negative assertions.
- No database schema push, backend, auth, provider, or ORM tasks are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ground-up-shadcn-ui-rebuild/07.1-05-SUMMARY.md`.
</output>
