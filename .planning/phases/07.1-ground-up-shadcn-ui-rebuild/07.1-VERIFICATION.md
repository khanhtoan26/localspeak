---
phase: 07.1-ground-up-shadcn-ui-rebuild
status: human_needed
verified_at: 2026-05-11T07:12:57Z
requirements_verified: 8
must_haves_verified: 45
gaps: 0
human_verification_required: 2
score: "8/8 roadmap success criteria automated-verified"
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Visual workbench fit check"
    expected: "The rebuilt app feels like a clean light-neutral shadcn SaaS workbench, not the rejected warm/premium/hero dashboard frame."
    why_human: "Automated checks verify tokens, layout structure, absence of rejected patterns, and contrast, but visual quality and perceived polish require human judgment."
  - test: "Real microphone Live Audio smoke test"
    expected: "With microphone permission and the configured transcription provider available, entering a reference sentence, recording, stopping, seeing transcript text, and receiving a pronunciation result all work in the browser."
    why_human: "Unit tests mock the audio session and E2E covers no-reference/ready states; real-time microphone/provider behavior requires an interactive browser and external service."
---

# Phase 07.1: Ground-Up Shadcn UI Rebuild Verification Report

**Phase Goal:** Supersede the rejected Phase 7 frame with a ground-up shadcn/ui app shell, design system, and redesigned JSON/audio surfaces.  
**Status:** human_needed  
**Blocking Gaps:** 0

## Summary

Automated verification found no blocking gaps against the Phase 07.1 roadmap success criteria, UIX-01 through UIX-08, locked design decisions, plan must-haves, and review-fix requirements.

The implementation supersedes the rejected Phase 7 frame:

- `apps/web/app/page.tsx` is a thin entry rendering `LocalSpeakWorkspace`.
- The shell uses shadcn `SidebarProvider`, `Sidebar`, `SidebarInset`, `SidebarTrigger`, grouped navigation, and mobile Sheet/drawer behavior.
- JSON Analysis is outcome-first and preserves validation, preview, analyze, warnings, stale-result state, tabs, saved sessions, AI feedback, sample load, upload, clear, and Zod parsing.
- Live Audio Practice is a focused sentence-practice flow preserving reference text, recording state, transcript display, and scoring behavior.
- The light-neutral token system is in `globals.css`; rejected premium/warm/radial/hero patterns are absent from production app code.
- Code-review fixes are present in source and covered by tests.

Status is `human_needed` rather than `passed` because two categories are inherently manual: visual polish judgment and real microphone/provider Live Audio behavior.

## Requirements Traceability

| Requirement | Status | Evidence |
|---|---:|---|
| UIX-01 — Cohesive app shell/navigation across JSON, Live Audio, future practice surfaces | VERIFIED | `LocalSpeakWorkspace` composes `SidebarProvider`, `AppSidebar`, `SidebarInset`, `AppHeader`, and `SurfaceRenderer`. `nav-config.ts` includes enabled JSON/Audio plus disabled IELTS/TOEIC future entries. |
| UIX-02 — Primary next action clear on major screens | VERIFIED | JSON empty state centers `Analyze Pronunciation`; JSON results begin with `What should I practice next?`; Audio no-reference state disables recording with guidance; Audio ready state exposes `Start Recording`. |
| UIX-03 — Documented design system for tokens/components/states | VERIFIED | `globals.css` defines light-neutral shadcn tokens; product components live under `components/design-system`; generated primitives remain under `components/ui`. |
| UIX-04 — JSON Analysis outcome-first redesign | VERIFIED | `json-analysis-panel.tsx` orders result state as priority action, metric summary, evidence tabs, saved sessions, and collapsed JSON input while preserving API/Zod behavior. |
| UIX-05 — Live Audio simple speaking flow | VERIFIED | `audio-mode-panel.tsx` orders reference sentence, readiness guidance, `RecordingControl`, live transcript, and pronunciation result. |
| UIX-06 — Phone-width responsive layout without overflow/hidden primary actions | VERIFIED | `responsive.spec.ts` checks 390px overflow for shell, JSON, tabs, and audio; primary recording action has a usable touch target. |
| UIX-07 — Accessibility basics | VERIFIED | shadcn/Radix tabs and sidebar semantics are preserved; labels exist for JSON input, upload, reference sentence; focus and contrast are covered in `responsive.spec.ts`. |
| UIX-08 — Unit/Playwright coverage for responsive and core flows | VERIFIED | Unit and E2E suites cover shell, JSON, audio, responsive, accessibility, anti-reuse guards, and review-fix regressions. |

## Must-Haves

| Must-have | Status | Evidence |
|---|---:|---|
| App shell uses shadcn Sidebar/SidebarProvider/SidebarInset rather than the old custom desktop rail and mobile bottom nav frame. | VERIFIED | `local-speak-workspace.tsx` imports `SidebarProvider`/`SidebarInset`; `app-sidebar.tsx` uses shadcn Sidebar pieces; anti-pattern scans found no production bottom nav/radial/premium frame. |
| Navigation is config-driven and ready for JSON Analysis, Live Audio Practice, and future IELTS/TOEIC practice surfaces. | VERIFIED | `nav-config.ts` separates enabled and coming-soon surfaces; disabled IELTS/TOEIC entries do not switch content. |
| JSON Analysis is rebuilt outcome-first with shadcn cards/tabs/collapsibles while preserving behavior. | VERIFIED | `json-analysis-panel.tsx`, `result-tabs.tsx`, and saved-session components preserve validation, analysis, warnings, saved sessions, AI feedback, and stale state. |
| Live Audio Practice is rebuilt as a simple speaking flow while preserving behavior. | VERIFIED | `audio-mode-panel.tsx` preserves `useDeepgramSession(referenceText)`, recording state, transcript display, and `scorePronunciation` behavior using the locked attempt reference. |
| Design system uses documented tokens and product-level composed components rather than one-off hard-coded frame styles. | VERIFIED | `globals.css`, `components/ui/*`, and `components/design-system/*` separate primitive and product concerns. |
| Phone-width layout has no horizontal overflow and no hidden primary actions. | VERIFIED | `responsive.spec.ts` covers 390px shell, JSON, tabs, audio, and recording control usability. |
| Keyboard navigation, focus states, semantic landmarks, touch targets, labels, and contrast are covered. | VERIFIED | `SidebarInset` is the semantic `main`; tests cover tab keyboarding, focus, labels, contrast, and touch target constraints. |
| Unit and Playwright coverage is updated for the new shadcn shell and preserved JSON/audio behavior. | VERIFIED | `page.test.tsx`, JSON tests, audio tests, `dashboard-ui.spec.ts`, and `responsive.spec.ts` cover the rebuilt contracts. |

## Review Fix Verification

| Finding | Status | Evidence |
|---|---:|---|
| WR-001 — Live Audio can rescore an old transcript against a newly edited reference sentence | FIXED | `audio-mode-panel.tsx` captures an attempt reference before recording and hides stale completed scores when the reference changes; `audio-mode-panel.test.tsx` covers stale-score behavior. |
| WR-002 — Mobile Sidebar remains open after selecting an enabled navigation item | FIXED | `app-sidebar.tsx` uses `useSidebar().setOpenMobile(false)` for enabled mobile selections; `responsive.spec.ts` covers drawer close after navigation. |
| WR-003 — Coming-soon Sidebar tooltips are not usable when desktop Sidebar is collapsed | FIXED | Coming-soon entries preserve `aria-disabled` semantics without native-disabled tooltip blocking; unit/E2E coverage checks disabled semantics and collapsed readability. |

## Automated Checks

| Gate | Status | Evidence |
|---|---:|---|
| Code review fix gate | PASS | `07.1-REVIEW-FIX.md` reports `all_fixed`, 3 fixed, 0 skipped. |
| Regression gate | PASS | Temporary Postgres-backed `pnpm test` passed, then `pnpm --filter web test:e2e` passed. |
| Schema drift gate | PASS | `verify.schema-drift "07.1"` returned `drift_detected=false`. |
| Codebase drift gate | SKIPPED | `verify.codebase-drift` skipped with reason `no-structure-md`. |

## Human Verification Required

### 1. Visual workbench fit check

**Test:** Open the app at desktop and phone widths. Review JSON Analysis empty/result states and Live Audio no-reference/ready states.  
**Expected:** The app feels like a clean, light-neutral shadcn SaaS workbench with restrained accent usage, not the rejected warm/premium/hero dashboard frame.  
**Why human:** Automated checks verify tokens, absence of rejected patterns, contrast, copy, and layout structure, but visual polish and subjective design fit require human judgment.

### 2. Real microphone Live Audio smoke test

**Test:** In a browser with microphone permission and the configured transcription provider available, enter a reference sentence, click `Start Recording`, speak the sentence, stop recording, and confirm transcript/scoring behavior.  
**Expected:** Recording state changes are clear, transcript appears, stale score does not persist after changing reference, and pronunciation result appears only for the recorded attempt reference.  
**Why human:** Unit tests mock `useDeepgramSession`; E2E verifies no-reference/ready UI states, but real microphone/provider integration requires an interactive browser and external service.

## Gaps Summary

No blocking implementation gaps were found.

The only remaining verification needs are human-centered:

1. Visual/polish judgment of the rebuilt shadcn workbench.
2. Real microphone/provider Live Audio smoke test.

## Recommendation

Proceed to phase completion after human verification of visual fit and real Live Audio microphone behavior.

_Verified: 2026-05-11T07:12:57Z_
