---
phase: 07.1-ground-up-shadcn-ui-rebuild
plan: "04"
subsystem: ui
tags: [audio, shadcn, recording, transcript, responsive, accessibility]

# Dependency graph
requires:
  - phase: 07.1-01
    provides: shadcn primitives, light neutral tokens, and recording/readiness product components
  - phase: 07.1-02
    provides: mounted-once shadcn app shell surface switching
  - phase: 07.1-03
    provides: token-first feature composition patterns
provides:
  - Focused Live Audio sentence-practice flow
  - shadcn-composed recording control with status badge, progress, and waveform area
  - Card-based transcript, error, and pronunciation result presentation
affects:
  - 07.1-05 unit and E2E hardening

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Live Audio order is reference text, readiness guidance, recording control, transcript, then pronunciation result"
    - "Recording controls compose shadcn Card/Button/Badge/Progress instead of custom primitive classes"
    - "Transcript and scoring output render as React text/nodes only"

key-files:
  created: []
  modified:
    - apps/web/components/audio-mode/audio-mode-panel.tsx
    - apps/web/components/audio-mode/live-analysis-panel.tsx
    - apps/web/components/design-system/recording-control.tsx
  deleted:
    - apps/web/components/audio-mode/record-button.tsx

key-decisions:
  - "Kept the existing Deepgram session hook and deterministic pronunciation scoring unchanged."
  - "Kept Live Audio fully single-column by default to avoid phone-width overflow."
  - "Deleted the legacy RecordButton once AudioModePanel stopped importing it."

patterns-established:
  - "Use PracticeReadinessCard to separate readiness guidance from recording controls."
  - "Use StatePanel for user-facing audio error states while preserving the underlying hook error as detail."
  - "Use semantic success/warning/destructive token classes for audio result chips and scores."

requirements-completed: [UIX-02, UIX-05, UIX-06, UIX-07, UIX-08]

# Metrics
duration: 4min
completed: 2026-05-11
---

# Phase 07.1 Plan 04: Live Audio Visual Rebuild Summary

**Focused sentence-practice Live Audio surface with shadcn reference input, readiness guidance, recording control, transcript, and pronunciation result**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-11T13:39:45+07:00
- **Completed:** 2026-05-11T06:43:37Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Rebuilt Live Audio into the required single-column order: reference sentence, readiness guidance, recording control, live transcript, and pronunciation result.
- Replaced the legacy `RecordButton` with the product `RecordingControl` composed from shadcn `Card`, `Button`, `Badge`, and `Progress`.
- Reworked transcript, error, and result presentation with shadcn/product panels, semantic tokens, wrapping word chips, and no hard-coded audio feature colors.
- Preserved `useDeepgramSession(referenceText)`, `scorePronunciation(transcript.words, referenceText)`, reference-text disabling, transcript state, and start/stop behavior.

## Task Commits

Each task was committed atomically:

1. **Task 1: Recompose AudioModePanel into the required sentence-practice order** - `2e58dcd` (feat)
2. **Task 2: Replace custom record button usage with shadcn-composed RecordingControl** - `c82908c` (feat)
3. **Task 3: Rework transcript, error, and pronunciation result presentation** - `c628b15` (feat)

**Plan metadata:** committed with SUMMARY.md

## Files Created/Modified

- `apps/web/components/audio-mode/audio-mode-panel.tsx` - Live Audio behavior owner and ordered sentence-practice composition.
- `apps/web/components/audio-mode/live-analysis-panel.tsx` - Card-based live transcript with polite updates and required empty copy.
- `apps/web/components/design-system/recording-control.tsx` - shadcn-composed recording control with required button/status copy and progress/waveform area.
- `apps/web/components/audio-mode/record-button.tsx` - Deleted legacy custom recording primitive after removing all imports.

## Decisions Made

- Did not change the backend/token/provider path; the current audio surface still uses the existing Deepgram session hook.
- Did not add any future exam-practice cockpit UI to Live Audio; the surface remains focused on one reference sentence and one recorded attempt.
- Kept the actual hook error as supporting detail while showing the required user-facing error copy: `We couldn't start recording. Check microphone permission and try again.`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

- Required Live Audio copy strings exist.
- `AudioModePanel` imports and renders `RecordingControl` and no longer imports `RecordButton`.
- `record-button.tsx` is deleted.
- `dangerouslySetInnerHTML` is absent from `apps/web/components/audio-mode`.
- Hard-coded six-digit hex colors are absent from `apps/web/components/audio-mode`.
- `pnpm --filter web check` passes.
- `pnpm --filter web test` passes.

## Next Phase Readiness

Plan 05 can update unit and E2E coverage against the rebuilt shadcn shell, JSON Analysis, and Live Audio surfaces.

---
*Phase: 07.1-ground-up-shadcn-ui-rebuild*
*Completed: 2026-05-11*
