---
phase: 07.1-ground-up-shadcn-ui-rebuild
plan: "01"
subsystem: ui
tags: [shadcn, tailwind-v4, design-system, sidebar, tokens, accessibility]

# Dependency graph
requires: []
provides:
  - Official shadcn primitives for Sidebar, Sheet, ScrollArea, Breadcrumb, Progress, Alert, and Label
  - Light neutral Tailwind v4 token system matching the approved Phase 07.1 UI-SPEC
  - Product-level composed design-system components for action banners, metrics, state panels, evidence cards, recording controls, and readiness cards
affects:
  - 07.1-02 app shell
  - 07.1-03 JSON Analysis rebuild
  - 07.1-04 Live Audio rebuild
  - 07.1-05 UI regression tests

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Generated shadcn primitives stay in components/ui with no product behavior"
    - "Product composition lives in components/design-system and imports shadcn primitives"
    - "Global styling is limited to Tailwind imports, theme tokens, and resets"

key-files:
  created:
    - apps/web/components/ui/sidebar.tsx
    - apps/web/components/ui/sheet.tsx
    - apps/web/components/ui/scroll-area.tsx
    - apps/web/components/ui/breadcrumb.tsx
    - apps/web/components/ui/progress.tsx
    - apps/web/components/ui/alert.tsx
    - apps/web/components/ui/label.tsx
    - apps/web/hooks/use-mobile.ts
    - apps/web/components/design-system/action-banner.tsx
    - apps/web/components/design-system/metric-card.tsx
    - apps/web/components/design-system/state-panel.tsx
    - apps/web/components/design-system/evidence-card.tsx
    - apps/web/components/design-system/recording-control.tsx
    - apps/web/components/design-system/practice-readiness-card.tsx
  modified:
    - apps/web/app/globals.css

key-decisions:
  - "Added the official shadcn use-mobile hook dependency required by Sidebar even though it was not listed in files_modified; the generator would create this dependency."
  - "Kept product-specific UI composition outside components/ui to preserve shadcn registry safety."

patterns-established:
  - "Token-first styling: feature components should use semantic tokens instead of hex literals."
  - "Design-system components accept content/variant props but do not own feature behavior or API calls."

requirements-completed: [UIX-03, UIX-06, UIX-07]

# Metrics
duration: 2min
completed: 2026-05-11
---

# Phase 07.1 Plan 01: Shadcn Primitive and Design-System Foundation Summary

**Official shadcn primitives, light-neutral Tailwind tokens, and product-level composition components for the Phase 07.1 app shell, JSON, and audio rebuilds**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-11T13:26:58+07:00
- **Completed:** 2026-05-11T06:29:21Z
- **Tasks:** 3
- **Files modified:** 15 (14 created, 1 modified)

## Accomplishments

- Added the required shadcn primitive sources for Sidebar, Sheet, ScrollArea, Breadcrumb, Progress, Alert, and Label, plus the Sidebar mobile breakpoint hook.
- Replaced the rejected warm premium theme with the approved light neutral token system: `#FAFAFA` background, `#FFFFFF` cards, `#2563EB` primary, and semantic success/warning/destructive tokens.
- Added reusable product-level design-system components under `components/design-system` for later JSON Analysis and Live Audio composition.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add required shadcn primitives without product logic** - `043fd46` (feat)
2. **Task 2: Replace globals with light neutral shadcn tokens** - `1e52530` (style)
3. **Task 3: Add product-level composed design-system components** - `49ec4de` (feat)

**Plan metadata:** committed with SUMMARY.md

## Files Created/Modified

- `apps/web/components/ui/sidebar.tsx` - Official shadcn Sidebar primitive adapted to local aliases.
- `apps/web/components/ui/sheet.tsx` - shadcn Sheet primitive for mobile drawer behavior.
- `apps/web/components/ui/scroll-area.tsx` - shadcn ScrollArea primitive for scrollable panels.
- `apps/web/components/ui/breadcrumb.tsx` - shadcn Breadcrumb primitive for shell header context.
- `apps/web/components/ui/progress.tsx` - shadcn Progress primitive for recording/status flows.
- `apps/web/components/ui/alert.tsx` - shadcn Alert primitive for validation/error states.
- `apps/web/components/ui/label.tsx` - shadcn Label primitive for accessible form labels.
- `apps/web/hooks/use-mobile.ts` - Official shadcn mobile breakpoint hook required by Sidebar.
- `apps/web/app/globals.css` - Light neutral Tailwind v4 tokens and global resets.
- `apps/web/components/design-system/action-banner.tsx` - Neutral CTA/status banner composition.
- `apps/web/components/design-system/metric-card.tsx` - Metric card composition with existing test id hook.
- `apps/web/components/design-system/state-panel.tsx` - Alert-backed status panel composition.
- `apps/web/components/design-system/evidence-card.tsx` - Compact evidence card composition.
- `apps/web/components/design-system/recording-control.tsx` - shadcn-composed recording control shell.
- `apps/web/components/design-system/practice-readiness-card.tsx` - Readiness guidance card composition.

## Decisions Made

- Used official shadcn registry source recovered through the registry fetch path after the CLI failed on local TLS certificate validation.
- Added `apps/web/hooks/use-mobile.ts` because the official Sidebar primitive depends on it for mobile drawer behavior.
- Kept product-level components behavior-free: no API calls, owner keys, persistence, provider keys, or feature-specific hex colors.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Recovered shadcn registry source after local TLS failure**
- **Found during:** Task 1 (Add required shadcn primitives without product logic)
- **Issue:** `pnpm dlx shadcn@latest add sidebar sheet scroll-area breadcrumb progress alert label` failed because the shell environment rejected the official registry TLS certificate chain.
- **Fix:** Fetched the official new-york-v4 registry item payloads and wrote the same primitive source locally with project aliases.
- **Files modified:** `apps/web/components/ui/*`, `apps/web/hooks/use-mobile.ts`
- **Verification:** All primitive files exist, forbidden product strings are absent from primitives, and `pnpm --filter web check` passes.
- **Committed in:** `043fd46`

---

**Total deviations:** 1 auto-fixed (Rule 3 — environment/tooling blocker).
**Impact on plan:** No scope expansion. The generated-source intent is preserved with official registry content and local alias adaptation.

## Issues Encountered

- The shadcn CLI failed on the local TLS chain. The implementation used official registry payloads instead of disabling TLS verification.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

- Required shadcn primitives exist.
- `components/ui/*` contains no LocalSpeak product strings, JSON/audio API calls, or feature behavior.
- `globals.css` contains the approved light neutral token values and no `font-display`, `Instrument Serif`, radial gradient, or warm premium accent.
- Product-level design-system components exist outside `components/ui` and contain no feature-level hex literals.
- `pnpm --filter web check` passes.

## Next Phase Readiness

Plan 02 can build the official shadcn Sidebar app shell on top of these primitives and tokens.

---
*Phase: 07.1-ground-up-shadcn-ui-rebuild*
*Completed: 2026-05-11*
