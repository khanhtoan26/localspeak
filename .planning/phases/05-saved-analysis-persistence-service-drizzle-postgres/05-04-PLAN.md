---
phase: 05-saved-analysis-persistence-service-drizzle-postgres
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - apps/api/src/saved-sessions/saved-sessions.controller.ts
  - apps/api/src/saved-sessions/saved-sessions.module.ts
  - apps/api/src/app.module.ts
  - apps/api/test/saved-sessions.e2e-spec.ts
  - apps/api/.env.example
  - README.md
autonomous: true
requirements: [STORE-01, STORE-02, ARCH-03]
must_haves:
  truths:
    - "Backend exposes POST /saved-sessions for explicit saves only."
    - "Backend exposes GET /saved-sessions?ownerKey=... for ownerKey-scoped lists."
    - "Backend exposes GET /saved-sessions/:id?ownerKey=... for ownerKey-scoped fetches."
    - "Existing JSON/audio analysis endpoints are not modified to auto-save."
    - "Docs explain DATABASE_URL, local Postgres, and Drizzle migration commands."
  artifacts:
    - path: "apps/api/src/saved-sessions/saved-sessions.controller.ts"
      provides: "HTTP routes for saved sessions"
      exports: ["SavedSessionsController"]
    - path: "apps/api/src/saved-sessions/saved-sessions.module.ts"
      provides: "NestJS feature module"
      exports: ["SavedSessionsModule"]
    - path: "apps/api/test/saved-sessions.e2e-spec.ts"
      provides: "Real Postgres e2e tests"
      contains: "wrong owner"
    - path: "README.md"
      provides: "Local Postgres and migration documentation"
      contains: "DATABASE_URL"
  key_links:
    - from: "SavedSessionsController"
      to: "SavedSessionsService"
      via: "create/list/get method delegation"
      pattern: "this.savedSessionsService"
    - from: "SavedSessionsModule"
      to: "AppModule"
      via: "Nest imports array"
      pattern: "SavedSessionsModule"
    - from: "README.md"
      to: "apps/api/package.json"
      via: "documented Drizzle scripts"
      pattern: "pnpm --filter api db:migrate"
    - from: "apps/api/test/saved-sessions.e2e-spec.ts"
      to: "saved_analysis_sessions"
      via: "real Postgres route tests after migration"
      pattern: "DATABASE_URL is required for saved-sessions e2e tests"
---

<objective>
Expose saved-session API routes, wire the module into the app, add real Postgres e2e coverage, and document local database setup.

Purpose: Complete Phase 5 backend/contracts/persistence delivery while preserving locked scope: no auth, no learner-facing UI, no auto-save in existing analysis endpoints.
Output: SavedSessionsController/module/AppModule wiring, e2e tests, env example, README database docs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-CONTEXT.md
@.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-RESEARCH.md
@.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-PATTERNS.md
@apps/api/src/app.module.ts
@apps/api/src/json-analysis/json-analysis.controller.ts
@apps/api/src/gemini-feedback/gemini-feedback.controller.ts
@apps/api/test/json-analysis.e2e-spec.ts
@apps/api/.env.example
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SavedSessionsController/module and wire AppModule</name>
  <files>apps/api/src/saved-sessions/saved-sessions.controller.ts, apps/api/src/saved-sessions/saved-sessions.module.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - apps/api/src/saved-sessions/saved-sessions.service.ts
    - apps/api/src/database/database.module.ts
    - apps/api/src/json-analysis/json-analysis.controller.ts
    - apps/api/src/gemini-feedback/gemini-feedback.controller.ts
    - apps/api/src/app.module.ts
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-CONTEXT.md
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-PATTERNS.md
  </read_first>
  <behavior>
    - POST `/saved-sessions` delegates to service `create`.
    - GET `/saved-sessions?ownerKey=...` delegates to service `listByOwnerKey`.
    - GET `/saved-sessions/:id?ownerKey=...` delegates to service `getByIdForOwner`.
    - Controller accepts raw `unknown` body and query/param strings; service validates.
    - No auth guards are added.
    - Existing analysis endpoints are not modified and do not auto-save per D-01/D-02.
    - Existing non-DB e2e tests can still compile `AppModule` without `DATABASE_URL` because Plan 05-03 uses lazy database initialization.
  </behavior>
  <action>
    Create `apps/api/src/saved-sessions/saved-sessions.controller.ts`:
    - import `Body`, `Controller`, `Get`, `HttpCode`, `Inject`, `Param`, `Post`, `Query`
    - `@Controller("saved-sessions")`
    - inject `SavedSessionsService`
    - `@Post()` + `@HttpCode(201)` method `create(@Body() body: unknown)`
    - `@Get()` method `list(@Query("ownerKey") ownerKey: unknown)`
    - `@Get(":id")` method `get(@Param("id") id: unknown, @Query("ownerKey") ownerKey: unknown)`

    Create `apps/api/src/saved-sessions/saved-sessions.module.ts`:
    - import `Module`
    - import `DatabaseModule`
    - provide `SavedSessionsService`
    - controller `SavedSessionsController`

    Update `apps/api/src/app.module.ts`:
    - import `SavedSessionsModule` from `./saved-sessions/saved-sessions.module`
    - add `SavedSessionsModule` to imports array

    Do not edit `apps/api/src/json-analysis/*`, `apps/api/src/gemini-feedback/*`, `apps/api/src/deepgram-token/*`, or frontend audio files. Phase 5 persistence is explicit save API only.
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && pnpm --filter api test:unit -- saved-sessions && env -u DATABASE_URL pnpm --filter api test:e2e -- --testMatch '**/test/(health|contracts|json-analysis).e2e-spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/saved-sessions/saved-sessions.controller.ts` contains `@Controller("saved-sessions")`
    - Controller file contains `@Post()`
    - Controller file contains `@Get()`
    - Controller file contains `@Get(":id")`
    - Controller file contains `@Query("ownerKey")`
    - `apps/api/src/saved-sessions/saved-sessions.module.ts` contains `imports: [DatabaseModule]`
    - `apps/api/src/app.module.ts` contains `SavedSessionsModule`
    - `grep -R "SavedSessionsService" apps/api/src/json-analysis apps/api/src/gemini-feedback apps/api/src/deepgram-token` returns no matches
    - `env -u DATABASE_URL pnpm --filter api test:e2e -- --testMatch '**/test/(health|contracts|json-analysis).e2e-spec.ts'` passes
  </acceptance_criteria>
  <done>Saved-session HTTP routes are wired into the backend app without auth guards and without auto-saving from existing analysis endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real Postgres e2e tests for create/list/fetch scoping</name>
  <files>apps/api/test/saved-sessions.e2e-spec.ts</files>
  <read_first>
    - apps/api/test/json-analysis.e2e-spec.ts
    - apps/api/src/app.module.ts
    - packages/contracts/src/saved-session.ts
    - apps/api/src/database/schema.ts
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-VALIDATION.md
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-PATTERNS.md
  </read_first>
  <behavior>
    - E2E requires real `DATABASE_URL`; it must not use sqlite, mocks, or in-memory fallback.
    - Create persists input mode, metadata, metrics, feedback, timestamps.
    - List returns only records for provided ownerKey.
    - Fetch by ID returns only when ownerKey matches.
    - Wrong owner returns 404.
    - Missing/short ownerKey returns 400.
    - Responses do not contain `speechAssessment`, `DATABASE_URL`, stack traces, or secret names.
  </behavior>
  <action>
    Create `apps/api/test/saved-sessions.e2e-spec.ts` using the same Nest app factory style as `json-analysis.e2e-spec.ts`.

    Required imports:
    - `NestExpressApplication`
    - `Test`
    - `request` from `supertest`
    - `SavedSessionCreateResponseSchema`, `SavedSessionListResponseSchema`, `SavedSessionDetailResponseSchema` from `@localspeak/contracts`
    - `AppModule`

    Add `beforeAll` guard:
    - if `!process.env.DATABASE_URL`, throw `new Error("DATABASE_URL is required for saved-sessions e2e tests")`

    App setup:
    - `Test.createTestingModule({ imports: [AppModule] }).compile()`
    - `createNestApplication<NestExpressApplication>({ bodyParser: false, logger: false })`
    - `app.useBodyParser("json", { limit: "2mb" })`

    Test owner keys:
    - `ownerA = "local-owner-a-1234567890"`
    - `ownerB = "local-owner-b-1234567890"`

    Test payload:
    ```ts
    {
      ownerKey: ownerA,
      inputMode: "json",
      title: "Practice attempt",
      referenceText: "This is a pronunciation practice attempt.",
      inputMetadata: { source: "json-analysis", fileName: "sample.json" },
      metrics: {
        summary: { pronunciationBand: 7.5, fluencyBand: 7, wpm: 142 },
        pronunciation: { band: 7.5 },
        fluency: { band: 7, wpm: 142 }
      },
      feedback: { pronunciationBand: 7.5, fluencyBand: 7, summary: "Good control." }
    }
    ```

    Include e2e cases:
    1. POST `/saved-sessions` returns 201 and validates `SavedSessionCreateResponseSchema`
    2. GET `/saved-sessions?ownerKey=${ownerA}` includes created record
    3. GET `/saved-sessions?ownerKey=${ownerB}` does not include ownerA record
    4. GET `/saved-sessions/:id?ownerKey=${ownerA}` returns detail with snapshots
    5. GET `/saved-sessions/:id?ownerKey=${ownerB}` returns 404
    6. GET `/saved-sessions` returns 400
    7. POST payload with `metrics: { speechAssessment: {} }` returns 400

    Add helper `expectNoUnsafeDetails(value)` asserting serialized response does not contain:
    - `speechAssessment`
    - `DATABASE_URL`
    - `GEMINI_API_KEY`
    - `DEEPGRAM_API_KEY`
    - `Error:`
    - `at `
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak pnpm --filter api db:migrate && DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak pnpm --filter api test:e2e -- --testMatch '**/test/saved-sessions.e2e-spec.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/test/saved-sessions.e2e-spec.ts` contains `DATABASE_URL is required for saved-sessions e2e tests`
    - E2E file contains `.post("/saved-sessions")`
    - E2E file contains `.get(\`/saved-sessions?ownerKey=`
    - E2E file contains `wrong owner`
    - E2E file contains `speechAssessment`
    - E2E file parses `SavedSessionCreateResponseSchema`
    - E2E file parses `SavedSessionListResponseSchema`
    - E2E file parses `SavedSessionDetailResponseSchema`
    - Real Postgres command in verify passes
  </acceptance_criteria>
  <done>Real Postgres e2e coverage proves create/list/fetch behavior, ownerKey scoping, missing ownerKey validation, wrong-owner 404, and no raw vendor echo.</done>
</task>

<task type="auto">
  <name>Task 3: Document DATABASE_URL, local Postgres, and Drizzle migration commands</name>
  <files>apps/api/.env.example, README.md</files>
  <read_first>
    - apps/api/.env.example
    - README.md
    - apps/api/package.json
    - apps/api/drizzle.config.ts
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-CONTEXT.md
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-RESEARCH.md
  </read_first>
  <action>
    Update `apps/api/.env.example` to include:
    ```dotenv
    DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak
    ```

    Update `README.md`:
    - Replace Supabase-era Phase 1 wording with Drizzle/Postgres via `DATABASE_URL`.
    - In Backend API env block include `DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak`.
    - Add section `### Local Postgres and migrations`.
    - Document that Phase 5 requires a local Postgres database reachable via `DATABASE_URL`.
    - Provide Docker example:
      ```bash
      docker run --name localspeak-postgres -e POSTGRES_USER=localspeak -e POSTGRES_PASSWORD=localspeak -e POSTGRES_DB=localspeak -p 5432:5432 -d postgres:16
      ```
    - Document migration commands:
      ```bash
      pnpm --filter api db:generate
      pnpm --filter api db:migrate
      pnpm --filter api db:check
      ```
    - Document e2e command:
      ```bash
      DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak pnpm --filter api test:e2e -- --testMatch '**/test/saved-sessions.e2e-spec.ts'
      ```
    - State: "`ownerKey` is a temporary local-history partition, not authentication. Signup/login/account-linked history are deferred to backlog item 999.1."
    - State: "Existing JSON/audio analysis endpoints do not auto-save; clients call `POST /saved-sessions` explicitly after successful analysis."
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && grep -R "DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak" apps/api/.env.example README.md && grep -R "ownerKey.*not authentication" README.md && grep -R "POST /saved-sessions" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/.env.example` contains `DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak`
    - `README.md` contains `### Local Postgres and migrations`
    - `README.md` contains `pnpm --filter api db:generate`
    - `README.md` contains `pnpm --filter api db:migrate`
    - `README.md` contains `pnpm --filter api db:check`
    - `README.md` contains `POST /saved-sessions`
    - `README.md` contains `ownerKey`
    - `README.md` contains `not authentication`
    - `README.md` contains `999.1`
  </acceptance_criteria>
  <done>Local database setup, migration commands, explicit save semantics, and auth deferral are documented for developers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client -> SavedSessionsController | Untrusted body/query/path crosses into backend persistence API. |
| Backend API -> Postgres | Controller/service writes and reads from persistent database. |
| Docs/env -> developer machine | Local setup must not encourage secret leakage or fake DB behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-13 | Information Disclosure | GET `/saved-sessions/:id` | mitigate | E2E verifies wrong-owner fetch returns 404 and no global ID fetch exists. |
| T-05-14 | Tampering | POST `/saved-sessions` | mitigate | Service validates request via strict contracts; controller delegates unknown body only. |
| T-05-15 | Spoofing | `ownerKey` docs/API | accept | Document `ownerKey` is not authentication; auth deferred to backlog 999.1. |
| T-05-16 | Information Disclosure | e2e/API error responses | mitigate | E2E asserts no `DATABASE_URL`, API key names, stack traces, or raw `speechAssessment` in responses. |
| T-05-17 | Repudiation | auto-save ambiguity | mitigate | Existing analysis endpoints remain unchanged; persistence happens only through explicit `POST /saved-sessions`. |
</threat_model>

<verification>
Run:
```bash
pnpm --filter @localspeak/contracts test
pnpm --filter api test:unit -- saved-sessions
env -u DATABASE_URL pnpm --filter api test:e2e -- --testMatch '**/test/(health|contracts|json-analysis).e2e-spec.ts'
DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak pnpm --filter api db:migrate
DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak pnpm --filter api test:e2e -- --testMatch '**/test/saved-sessions.e2e-spec.ts'
DATABASE_URL=postgresql://localspeak:localspeak@localhost:5432/localspeak pnpm test
pnpm check
```
</verification>

<success_criteria>
- Routes `POST /saved-sessions`, `GET /saved-sessions?ownerKey=...`, and `GET /saved-sessions/:id?ownerKey=...` exist.
- Existing analysis endpoints do not auto-save.
- E2E tests pass against real Postgres.
- Existing non-DB e2e tests compile AppModule without DATABASE_URL.
- Full `pnpm test` passes with DATABASE_URL set to a real Postgres database.
- README and `.env.example` document `DATABASE_URL`, migrations, and ownerKey semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-04-SUMMARY.md`
</output>
