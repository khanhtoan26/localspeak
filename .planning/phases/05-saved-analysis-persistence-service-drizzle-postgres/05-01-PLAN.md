---
phase: 05-saved-analysis-persistence-service-drizzle-postgres
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/saved-session.ts
  - packages/contracts/test/saved-session.contract.test.ts
autonomous: true
requirements: [STORE-01, STORE-02, STORE-03]
must_haves:
  truths:
    - "Shared saved-session contracts require ownerKey for create/list/fetch and do not require authentication fields in create payloads."
    - "Contracts expose list summary and detail response shapes for Phase 6 history/reopen consumers."
    - "Contracts include nullable userId on stored/list/detail records for later auth migration."
    - "Create contracts reject obvious raw vendor speechAssessment storage by default."
  artifacts:
    - path: "packages/contracts/src/saved-session.ts"
      provides: "Strict Zod contracts for saved-session create/list/fetch APIs"
      contains: "SavedSessionCreateRequestSchema"
    - path: "packages/contracts/test/saved-session.contract.test.ts"
      provides: "Contract tests for ownerKey, summary/detail responses, and raw vendor rejection"
      contains: "rejects missing ownerKey"
  key_links:
    - from: "SavedSessionCreateRequestSchema"
      to: "SavedSessionsService.create"
      via: "safeParse request validation before insert"
      pattern: "SavedSessionCreateRequestSchema.safeParse"
    - from: "SavedSessionListResponseSchema"
      to: "GET /saved-sessions?ownerKey=..."
      via: "summary-only list API contract"
      pattern: "contract: z.literal(\"saved-session-list.v1\")"
    - from: "SavedSessionDetailResponseSchema"
      to: "GET /saved-sessions/:id?ownerKey=..."
      via: "full snapshot detail API contract"
      pattern: "contract: z.literal(\"saved-session-detail.v1\")"
---

<objective>
Create strict shared saved-session contracts for explicit save, ownerKey-scoped list, and ownerKey-scoped fetch.

Purpose: Give backend service/controllers and future Phase 6 UI a stable contract surface without implementing auth or learner-facing UI in Phase 5.
Output: Replaced saved-session contract shell plus Vitest coverage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-CONTEXT.md
@.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-RESEARCH.md
@.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-PATTERNS.md
@packages/contracts/src/saved-session.ts
@packages/contracts/src/json-analysis.ts
@packages/contracts/src/gemini-feedback.ts
@packages/contracts/src/audio-streaming.ts
@packages/contracts/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace saved-session shell with strict ownerKey-scoped contracts</name>
  <files>packages/contracts/src/saved-session.ts, packages/contracts/test/saved-session.contract.test.ts</files>
  <read_first>
    - packages/contracts/src/saved-session.ts
    - packages/contracts/src/json-analysis.ts
    - packages/contracts/src/gemini-feedback.ts
    - packages/contracts/src/index.ts
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-CONTEXT.md
    - .planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-PATTERNS.md
  </read_first>
  <behavior>
    - Missing `ownerKey` in create request fails validation per D-03/D-04.
    - `ownerKey` shorter than 16 characters fails validation per D-03/D-04.
    - Create payload does not accept `userId`; `userId` appears only as nullable future ownership field on stored/list/detail records per D-05/STORE-03.
    - Top-level `speechAssessment`, `rawSpeechAssessment`, and `vendorPayload` fields are rejected in `inputMetadata`, `metrics`, and `feedback` per D-08.
    - List response includes summary-only fields and no full snapshots.
    - Detail response includes full JSONB snapshots for reopen without recomputing per D-07.
  </behavior>
  <action>
    Replace `packages/contracts/src/saved-session.ts` with strict Zod schemas.

    Required exports:
    - `SavedSessionInputModeSchema = z.enum(["json", "audio"])`
    - `SavedSessionOwnerKeySchema = z.string().min(16).max(256)`
    - `SavedSessionJsonSnapshotSchema` as `z.record(z.string(), z.unknown()).superRefine(...)` that rejects top-level keys `speechAssessment`, `rawSpeechAssessment`, and `vendorPayload`
    - `SavedSessionCreateRequestSchema = z.strictObject({ ownerKey, inputMode, title, referenceText, inputMetadata, metrics, feedback })`
    - `SavedSessionListQuerySchema = z.strictObject({ ownerKey })`
    - `SavedSessionFetchParamsSchema = z.strictObject({ id: z.string().uuid(), ownerKey })`
    - `SavedSessionListItemSchema` with `id`, `ownerKey`, `userId: z.string().uuid().nullable()`, `inputMode`, `title`, `referenceText`, `pronunciationBand`, `fluencyBand`, `wpm`, `createdAt`, `updatedAt`
    - `SavedSessionListResponseSchema = z.strictObject({ contract: z.literal("saved-session-list.v1"), sessions: z.array(SavedSessionListItemSchema) })`
    - `SavedAnalysisSessionSchema` detail shape with all list fields plus `inputMetadata`, `metrics`, and nullable `feedback`
    - `SavedSessionCreateResponseSchema = z.strictObject({ contract: z.literal("saved-session-create.v1"), session: SavedAnalysisSessionSchema })`
    - `SavedSessionDetailResponseSchema = z.strictObject({ contract: z.literal("saved-session-detail.v1"), session: SavedAnalysisSessionSchema })`

    Export inferred TypeScript types for every exported schema above.

    Do not add signup/login/session/account-linking fields. Do not require auth in create payloads. Use nullable `userId` only on persisted/returned records per D-05 and STORE-03.
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && pnpm --filter @localspeak/contracts test -- saved-session</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/saved-session.ts` contains `SavedSessionOwnerKeySchema = z.string().min(16).max(256)`
    - `packages/contracts/src/saved-session.ts` contains `userId: z.string().uuid().nullable()`
    - `packages/contracts/src/saved-session.ts` contains `SavedSessionCreateRequestSchema = z.strictObject`
    - `packages/contracts/src/saved-session.ts` contains `SavedSessionListResponseSchema`
    - `packages/contracts/src/saved-session.ts` contains `SavedSessionDetailResponseSchema`
    - `packages/contracts/src/saved-session.ts` contains `speechAssessment`
    - `packages/contracts/src/saved-session.ts` does not define login/signup/session schemas
    - `pnpm --filter @localspeak/contracts test -- saved-session` passes
  </acceptance_criteria>
  <done>Saved-session contracts are strict, ownerKey-scoped, future-auth-ready via nullable userId, and reject raw vendor payload storage by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add saved-session contract tests for create/list/fetch payloads</name>
  <files>packages/contracts/test/saved-session.contract.test.ts</files>
  <read_first>
    - packages/contracts/test/speech-assessment.fixture.test.ts
    - packages/contracts/test/json-analysis.metrics.test.ts
    - packages/contracts/src/saved-session.ts
    - packages/contracts/src/json-analysis.ts
    - packages/contracts/src/gemini-feedback.ts
  </read_first>
  <behavior>
    - Valid JSON-mode create payload with metrics and feedback snapshots parses.
    - Valid audio-mode create payload with input metadata and feedback parses.
    - Missing/short ownerKey fails.
    - Create payload with userId fails.
    - Raw vendor field `speechAssessment` fails when sent inside snapshots.
    - List response validates summary-only fields.
    - Detail response validates full snapshots.
  </behavior>
  <action>
    Create `packages/contracts/test/saved-session.contract.test.ts` using Vitest.

    Import from `../src`:
    - `SavedSessionCreateRequestSchema`
    - `SavedSessionListResponseSchema`
    - `SavedSessionDetailResponseSchema`
    - `SavedSessionCreateResponseSchema`

    Test values:
    - `ownerKey: "local-owner-1234567890"`
    - `id: "11111111-1111-4111-8111-111111111111"`
    - `userId: null`
    - `inputMode: "json"` and `"audio"`
    - timestamps: `"2026-05-08T00:00:00.000Z"`
    - summary metrics: `pronunciationBand: 7.5`, `fluencyBand: 7`, `wpm: 142`

    Include explicit `expect(result.success).toBe(false)` tests for:
    - `{ inputMode: "json", inputMetadata: {}, metrics: {} }`
    - `{ ownerKey: "short", inputMode: "json", inputMetadata: {}, metrics: {} }`
    - create payload containing `userId`
    - create payload containing `metrics: { speechAssessment: {} }`

    Assert list item does not require `inputMetadata`, `metrics`, or `feedback`; assert detail response does include them.
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localspeak && pnpm --filter @localspeak/contracts test -- saved-session.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/test/saved-session.contract.test.ts` contains `describe("saved-session contracts"`
    - Test file contains `local-owner-1234567890`
    - Test file contains `speechAssessment`
    - Test file contains `expect(result.success).toBe(false)`
    - Test file validates both `SavedSessionListResponseSchema` and `SavedSessionDetailResponseSchema`
    - `pnpm --filter @localspeak/contracts test -- saved-session.contract.test.ts` passes
  </acceptance_criteria>
  <done>Contract tests prove saved-session create/list/fetch schemas support required persistence behavior and reject deferred-auth/raw-vendor anti-patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| client/tests -> shared contracts | Untrusted request payloads are parsed before service persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01 | Tampering | SavedSessionCreateRequestSchema | mitigate | Use `z.strictObject`, required `ownerKey`, and reject unexpected auth fields. |
| T-05-02 | Information Disclosure | saved-session snapshots | mitigate | Reject top-level raw vendor keys `speechAssessment`, `rawSpeechAssessment`, and `vendorPayload` by contract. |
| T-05-03 | Elevation of Privilege | `ownerKey` semantics | mitigate | Keep create payload free of `userId`; nullable `userId` is returned/persisted only for future migration and not treated as auth. |
</threat_model>

<verification>
Run:
```bash
pnpm --filter @localspeak/contracts test -- saved-session
pnpm --filter @localspeak/contracts build
```
</verification>

<success_criteria>
- Saved-session contracts are strict.
- Create requires `ownerKey`.
- List/fetch contract shapes exist.
- `userId` is nullable on persisted/returned records only.
- Raw vendor speech assessment storage is rejected by contract.
</success_criteria>

<output>
After completion, create `.planning/phases/05-saved-analysis-persistence-service-drizzle-postgres/05-01-SUMMARY.md`
</output>
